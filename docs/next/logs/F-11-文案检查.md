# F-11 文案检查（无证据安全结论审计）

执行时间：2026-09-05T03:11:31Z UTC
命令与结果如下。判据：产品用户可见文本中不得出现系统保证安全不存在 rootkit与磁盘一致所以安全一类无证据结论。

## 1. 中文断言式结论
```
$ grep -rn "不存在 rootkit\|没有 rootkit\|无 rootkit\|系统安全\|保证安全\|确保安全\|系统未被入侵\|未被感染\|绝对安全\|完全安全" --include=*.cpp --include=*.h --include=*.json Ksword5.1/Ksword5.1/ KswordARKLight/ shared/
Ksword5.1/Ksword5.1/OtherDock/OtherDock.DesktopCreate.cpp:79:        SECURITY_DESCRIPTOR descriptorStorage{}; // descriptorStorage：私有 DACL 模式下的绝对安全描述符。
shared/evidence/EvidenceEnvelope.h:56:    NoDifferenceObserved,   // 有覆盖足够的观测且未发现矛盾 —— 不是"系统安全"
shared/evidence/EvidenceEnvelope.h:161:// 结构化事实，调用方据此渲染；它绝不产出"系统安全"/"不存在 rootkit"结论。
```

命中 3 条，逐条判定：
- `OtherDock.DesktopCreate.cpp:79` —— "绝对安全描述符" 是 Win32 术语 absolute SECURITY_DESCRIPTOR，非安全结论。
- `shared/evidence/EvidenceEnvelope.h:56 / :161` —— 本轮新增代码的注释，内容恰是禁止该结论。

## 2. 英文断言式结论
```
$ grep -rni "no rootkit\|system is safe\|guaranteed safe\|is clean and safe\|not infected\|completely safe" --include=*.cpp --include=*.h --include=*.json Ksword5.1/Ksword5.1/ shared/
（无输出）
```

## 3. "与磁盘一致所以安全"

仅命中 KernelDock 知识库条目与一条驱动拒绝提示；前者的原文是**限制声明**（"与磁盘一致也不能证明签名、调用路径和运行时行为可信"），后者是 "驱动已安全拒绝" 即 failed safely，均非安全保证。

## 结论

当前工作树的用户可见文本中不存在无证据的安全保证结论。本轮新增的 `buildTrustStatement()` 只产出 limitationKeys，结构上无法生成正向安全断言。
