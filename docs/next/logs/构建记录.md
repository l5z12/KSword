# Q-03 构建范围和结果

工具链：MSBuild 17.14.51（VS 2022 BuildTools）、MSVC v143、Qt 6.9.3（`.deps\Qt\6.9.3\msvc2022_64`）、QtMsBuild 3.5.0.0、WDK WindowsKernelModeDriver10.0
配置：Release / x64

| 目标 | 命令 | 退出码 | 产物 | 日志 |
|---|---|---|---|---|
| KswordARKLightTests | `/t:Rebuild`（不复用旧产物） | 0 | `KswordARKLightTests\x64\Release\KswordARKLightTests.exe` | `KswordARKLightTests-run.txt` |
| Ksword5.1（Qt 主程序） | `/t:Build /p:QtMsBuild=.deps\QtVsTools\msbuild` | 0（0 编译错误） | `Ksword5.1\x64\Release\Ksword5.1.exe` | `qt-build-driverdock.txt` |
| KswordARKLight | `/t:Build` | 0 | `Ksword5.1\x64\Release\KswordARKLight.exe` | `light-cli-build.txt` |
| KswordCLI | `/t:Build` | 0 | `Ksword5.1\x64\Release\KswordCLI.exe` | `light-cli-build.txt` |
| KswordARKDriver | `/t:Build /p:KswordArkSkipAutoVariantSign=true` | 0，`Driver is 'Universal'.` | `Ksword5.1\x64\Release\KswordARKDriver\` + `kswordarkdriver.cat` | `driver-build.txt` |

驱动本轮**没有源码改动**（`KswordARKDriver/` 与 `shared/driver/` 零改动），构建它只是为了确认工具链完整、本轮改动没有牵动 R0。

## 新增源文件的工程登记

- `KswordARKLightTests.vcxproj` / `.filters`：新增 4 个测试 .cpp（`EvidenceContractTests` / `CrossViewTests` / `ImageIntegrityTests` / `MemoryEvidenceTests`）、`TestSupport.h`，以及 11 个 `shared\evidence\*.cpp` 与对应头。
- `Ksword5.1.vcxproj` / `.filters`：新增 7 个 `shared\evidence\*.cpp`（Qt 主程序需要链接 `formatU64` 等）与 `ArkDriverClient\ArkDriverEvidence.h`。
- 语言资源：`zh-CN.json` / `en-US.json` 各**定点新增 10 行**（5 条 key 词条 + 5 条整串源文本词条），无键序重排；`tools/i18n_language_pack.py audit` 通过（24289 条源串）。

## 与构建分开报告的三件事

1. **签名**：`signtool verify /pa` 对 `Ksword5.1.exe` 与 `KswordARK.sys` 均失败，原因是测试签名证书 `CN=KswordARK Test Signing Certificate` 未导入本机 LocalMachine 信任区。这是**既有状态**，本轮未改动签名流程，也**没有**导入证书或启用测试签名（超出本任务授权）。
2. **打包**：本轮未制作发行包。
3. **真实加载**：本轮未加载驱动、未做任何 R0 现场验证。

## 判据核对

- 没有复用旧产物冒充新实现：测试工程用 `/t:Rebuild`，其余目标的日志里能看到本轮改动文件被实际重新编译。
- 驱动后置校验（ApiValidator）通过（`Driver is 'Universal'.`），未把被中止的构建说成成功。
- 未让每个子 agent 重复全仓构建：子 agent 只做 `cl /c` 语法/警告自检，写到各自的 scratch 目录；所有 MSBuild 构建由集成 owner 串行执行。

---

## 修复轮之后的复核构建（最终状态）

四轮对抗性评审查出 56 条问题（6 条 blocker）并全部派修后，重新构建全部受影响目标：

| 目标 | 退出码 | 产物时间 |
|---|---|---|
| KswordARKLightTests（`/t:Rebuild`） | 构建 0 / 运行 0，**837 项断言全绿**（F 189 / X 169 / I 280 / M 199） | 09-05 00:53 |
| Ksword5.1（Qt 主程序） | 0，0 编译错误 | 09-05 00:50 |
| KswordARKLight | 0 | 09-05 00:51 |
| KswordCLI | 0（未重新链接：本轮改动不涉及它引用的翻译单元） | 09-04 23:32 |
| KswordARKDriver | 0，`Driver is 'Universal'.` | 09-04 23:4x |

Qt 主程序这次重建是必要的：`ArkDriverEvidence.h` 的 NTSTATUS 分流逻辑变了，而 `DriverDock` 的 14 处采集状态展示直接依赖它。

---

## P1/P2 落地后的最终构建

| 目标 | 命令 | 退出码 | 说明 |
|---|---|---|---|
| KswordARKLightTests | `/t:Rebuild` | 构建 0 / 运行 0 | **3,533 项断言全绿**（10 个套件） |
| Ksword5.1（Qt 主程序） | `/t:Build /p:KswordArkSkipAutoVariantSign=true /p:KswordArkSkipAutoTestSign=true` | 0，0 编译错误 | 构建期门禁一并通过：`i18n audit passed: 24388 source strings`、`theme token audit passed: 14 dynamic tokens, 660 source files` |
| KswordARKLight | `/t:Build` | 0 | |
| KswordCLI | `/t:Build` | 0 | |
| KswordARKDriver | `/t:Build`（本轮更早一次） | 0，`Driver is 'Universal'.` | 驱动源码本轮零改动 |

### 一次卡住的构建及其处置（如实记录）

03:32 启动的 Qt 构建在 12 分钟内没有进展。按 `.claude/memory` 里记的纪律核实：进程树是 `msbuild → cmd → powershell(Sign-KswordArkDriverVariant.ps1)`，该 powershell 12 分钟只消耗 1.2 秒 CPU，且**全机没有任何活跃的 `cl` / `link` / `signtool` / `ApiValidator` / `aitstatic`**。确认卡点在自动 variant 签名步骤之后，只结束了**这一次构建**的 signer 与它的宿主，随后用记录在案的 `KswordArkSkipAutoVariantSign=true` 重跑并通过。

**没有把被中止的构建说成成功**：上表的退出码来自重跑后的那一次。

### 另一个反复出现的现象

`Ksword5.1.exe` 在链接阶段多次报 `LNK1104: 无法打开文件`，但当时没有任何 KSword 进程在运行，文件也可以被重命名 —— 是共享读锁（构建末尾的签名步骤或本机杀软扫描）造成的瞬时占用。等待后重试即通过，**不是代码问题**，也没有为此改动任何构建配置。

## 签名 / 打包 / 真实加载（与构建分开报告，本轮均未完成）

1. **签名**：本轮最终构建**显式跳过**了自动测试签名（`KswordArkSkipAutoTestSign` / `KswordArkSkipAutoVariantSign`）。此前未跳过时 `signtool verify /pa` 对 `Ksword5.1.exe` 与 `KswordARK.sys` 均失败，原因是测试证书 `CN=KswordARK Test Signing Certificate` 未导入本机 LocalMachine 信任区。本轮**没有**导入证书，也**没有**启用测试签名 —— 那超出本任务授权。
2. **打包**：未制作发行包。
3. **真实加载**：未加载驱动，未做任何 R0 现场验证。
