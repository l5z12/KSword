# 下一阶段证据层：使用与限制说明

配套规范：[`KSword_Next_Roadmap_Acceptance.md`](KSword_Next_Roadmap_Acceptance.md)
逐项结果：[`KSword_Acceptance_Results_Template.json`](KSword_Acceptance_Results_Template.json)
实现映射：[`实现映射.md`](实现映射.md)
执行日志与证据：[`logs/`](logs)

本文件说明本轮交付的代码**怎么用**，以及它**证明不了什么**。

---

## 1. 这一层是什么

`shared/evidence/` 是一层纯 C++20、**不含 Qt、不含 Win32 头**的分析代码。它不采集数据，只负责描述"这次采集是怎么回事"和"从采集结果能推出什么"。Qt 主程序、KswordARKLight 和 KswordCLI 都能直接编译它，Light 的"不链接 Qt"边界不受影响。

它解决的是规范第 4 节点名的一件事：**把"采集失败"、"分析无法判断"和"正确的空集合"变成三个不同的值**，而不是三张一样的空表格。

## 2. 每个文件负责什么

| 文件 | 验收编号 | 作用 |
|---|---|---|
| `LosslessValue.h/.cpp` | F-08 | `OptionalU64`（"未知"和"零"是两个状态）、`formatU64`/`ParseU64`。地址、64 位 ID、计数、高精度时间戳全程不经过 `double`。 |
| `EvidenceJson.h/.cpp` | F-08、Q-12 | 无浮点 JSON 读写。带小数点或指数的 number **直接判失败**，不降精度。解析器有深度/长度/成员数/节点数硬上限，重复键拒绝。 |
| `EvidenceEnvelope.h/.cpp` | F-04、F-05、F-06、F-11 | `CollectionStatus`（7 态）、`CollectionOutcome`（保留原始错误码+domain+message）、`CoverageAccount`（请求/已处理范围、失败/跳过/截断、上限、总数未知）、`AnalysisConclusion`、`captureWindow`（UTC 与单调时钟分开）、`buildTrustStatement`。 |
| `ObjectIdentity.h/.cpp` | F-03 | 进程/线程/驱动/文件/句柄/连接六类生命周期身份，`MatchResult{NoMatch, Candidate, Confirmed}`，`crossSessionKey()`。 |
| `ScanBudget.h/.cpp` | F-09、F-10、M-10 | `LatestRequestGate`（旧结果不回填）、`validateRange`、`ScanBudget`/`evaluateBudget`、`decideRefresh`（会话数据不可被后台刷新覆盖）。 |
| `LiveNavigation.h/.cpp` | F-09、F-12 | 离线结果跳转现场前的身份重校验；导航请求的五种结果判定。 |
| `CrossViewDiff.h/.cpp` | X-01…X-08 | 多轮多视图差异引擎：来源分组、复采样状态机、失败≠缺项。 |
| `Ksword5.1/.../ArkDriverClient/ArkDriverEvidence.h` | F-05、Q-04 | `IoResult` → `CollectionOutcome` 的唯一翻译入口。header-only（该目录被 5 个工程各自编译）。 |
| `PeImageMap.h/.cpp` | I-02、I-03 | PE64 归一化核：文件偏移/RVA/VA/raw/virtual/零填充严格分开；节重叠、越界、截断按**节粒度**降级为不可比较而不是整映像失败；x64 重定位归一化，不支持的重定位类型只把受影响字节标不可比较。 |
| `ImageDiff.h/.cpp` | I-04、I-05、I-06、I-09、I-10 | 差异定位（含 readStatus 缺失标记，读不到的字节绝不补 00）、带 ruleId/ruleVersion 与生效 RVA 范围的解释规则、有界跳转跟随（深度/字节/环检测）、模块身份读前读后复核、参考来源信任说明。 |
| `PageTableWalk.h/.cpp` | M-03、M-04、M-05 | x64 四级页表解析核，物理读取走回调（离线夹具与真实 R0 共用签名）。**补上了 R0 目前缺的保留位校验**；非 present / 保留位异常 / 读失败一律不产出物理地址；软件 PTE 分 Transition/Prototype/PageFile/Zero/Unknown。 |
| `MemoryRegionEvidence.h/.cpp` | M-01、M-02、M-07、M-09 | 区域记录与来源标注（未走 VAD 就不允许显示"VAD 已验证"）、读取孔洞语义（孔洞不是 0）、可执行区域线索规则（只出事实列表，无恶意字段）、pool 归因三档。 |
| `TimelineCore.h/.cpp` | T-01…T-06、T-08…T-10、T-12 | 会话生命周期（**"暂停显示"与"停止采集"是两个状态**）、事件 envelope 保留 provider/eventId/version/opcode/原始字段（未知 schema 不套旧结构）、跨源排序保留源时间与接收时间、事件绑定进程实例（缺开始事件时建临时实体而不是归给同 PID 进程）、六类丢失分开计数、流式落盘与版本化恢复、边类型区分（因果措辞只给来源直接提供的关联）。 |
| `WfpAnalysis.h/.cpp` | N-01…N-06、N-08 | WFP 对象按 GUID 而非本地化名称关联、条件类型完整解释（**不认识的条件绝不当"无条件匹配"**）、静态候选三态且按 layer/sublayer 分组（不按单一权重排序）、实际事件与静态候选在结构上分开、归因三档、动态变化与旧 id 复用的失效状态。 |
| `SecurityState.h/.cpp` | S-01…S-07 | 每个能力四个独立维度（硬件支持/配置/运行/查询结果），**Configured 与 Running 不可混用**、**查询失败 ≠ 关闭**、未知枚举值原样保留、跨 bootId 的状态标为历史、多来源不一致时同时展示且不自动选有利值、权限要求精确到字段。 |
| `SnapshotCompare.h/.cpp` | D-01…D-07 | 快照带范围与来源（**选择范围不同不得解释成对象被删除**）、按实体身份的语义比较键、地址按"映像身份+RVA"归一化（只在确认可比较时）、来源失败判 NotComparable 而**不报"全部对象已移除"**、变化事实与风险解释分开、版本化持久化、脱敏保持一致映射且原值不残留。 |
| `DumpFacts.h/.cpp` | C-02…C-10 | 从头部字节识别转储类型（**不按扩展名硬解析**、不把用户态 minidump 当内核 dump）、崩溃事实三态（缺字段不填 0）、符号匹配四态（**错版 PDB 不给行号**）、栈展开状态四分（不拼接猜测帧）、可疑模块三类证据分开（**不把 ntoskrnl 或最后一帧当根因**、无责任百分比字段）、small dump 的内容可用性上界、报告转义与命令白名单。 |
| `EntityGraph.h/.cpp` | G-01…G-06、G-08 | 边带类型/方向/时间有效区间/证据引用/确定性（**缺证据的边不得为 Confirmed**）、不同启动周期与地址复用是不同节点、三条调查链含"缺失"环节、有界展开（默认 200 节点/500 边，计数不暗示已加载全部）、孤立与未知四分（**无"无边就恶意"规则**）、离线展开只用已保存数据、打乱输入顺序结论不变。 |

## 3. 怎么用

### 3.1 记录一次采集

```cpp
using namespace Ksword::Evidence;

EvidenceEnvelope envelope;
envelope.source = ksword::ark::makeDriverSource(
    "ui.driverTable",              // 这一层包装的 id
    1U,                            // 解析版本
    "r0.module.enum",              // 底层证据来源 —— 同一 collector 的多层包装必须共用
    "IOCTL_KSWORD_ARK_QUERY_MODULES");
envelope.window.bootId    = currentBootId;   // 跨启动周期禁止拿单调时钟相减
envelope.window.machineId = currentMachineId;
envelope.window.mode      = CaptureMode::Snapshot;

const auto result = client.queryModules(...);
envelope.outcome = ksword::ark::toCollectionOutcome(result.io, {result.unsupported, result.partial});
envelope.coverage.succeeded = rows.size();
envelope.coverage.totalKnown = OptionalU64::unset();   // 不知道总数就写未知
```

### 3.2 得出结论（而不是自己判"正常"）

```cpp
const AnalysisConclusion conclusion = envelope.deriveConclusion(foundDifference);
```

`deriveConclusion` 的硬规则：采集状态不是 `Success`/`Partial` 时**一律返回 `NoEvidence`**；覆盖不完整时最多到 `Indeterminate`。**没有任何路径能从"没采到"推出"正常"。**

### 3.3 对象身份

```cpp
ProcessInstanceId id;
id.bootId = bootId;
id.pid = OptionalU64::of(pid);
id.createTime100ns = OptionalU64::of(createTime);   // 缺这个就只是 Weak

if (id.crossSessionKey().empty()) {
    // 身份不足：可以显示，但不能当跨会话主键，也不能自动跳转
}
switch (MatchProcessInstance(saved, live)) {
case MatchResult::Confirmed: /* 同一实例 */ break;
case MatchResult::NoMatch:   /* PID 被复用或跨启动 —— 拒绝把操作交给它 */ break;
case MatchResult::Candidate: /* 证据不足，保留候选，不要升级成确定关系 */ break;
}
```

### 3.4 Cross-view 差异

```cpp
std::vector<SampleRound> rounds = /* 每轮包含各视图的 ViewSnapshot */;
const CrossViewReport report = AnalyzeCrossView(rounds);
// report.trust.viewCount 与 report.trust.independentSourceGroupCount 必须一起显示
for (const CrossViewFinding& f : report.findings) {
    // f.state: NoDiscrepancy / PendingRecheck / Transient / Persistent / ObjectEnded / Unverifiable
    // f.latestHits 可展开到每个视图的命中情况和源记录 id
    // f.recheckHistory 是复核历史（含间隔）
}
```

`ViewSnapshot::coversTargetScope` 必须由采集方如实填写。它为假、或视图状态不是 `Success`、或覆盖账目不完整时，该视图**没有资格**参与"缺项"推断，结果显示 Unknown 而不是"确认不存在"。

## 4. 这一层证明不了什么

- **多个视图一致 ≠ 系统没被入侵。** `buildTrustStatement` 只产出 `limitationKeys`，结构上无法生成正向安全断言。同一个 collector 的两层包装被折叠成一个来源组，不会虚增可信度。
- **`Persistent` ≠ 恶意。** 它只表示"在满足条件的多轮复查中，成功且覆盖完整的视图始终没有列出这个对象"。判据里没有恶意分类，API 里也没有 malicious/threat 字段。
- **`NoDifferenceObserved` ≠ 正常。** 它只表示这些视图之间未发现矛盾，且覆盖足够。
- **读取本身有副作用。** 通过 `MmCopyMemory` 读目标内存会影响驻留状态和时序。任何"完全无观测副作用"的说法都是错的。
- **身份不足就是不足。** `Candidate` 不会因为再多几个弱证据就自动升级成 `Confirmed`。

## 5. 当前状态（重要）

**本轮交付的是分析层与其离线测试，不是完整的产品功能。**

已实际执行并通过：
- `KswordARKLightTests` Release x64 `/W4 /WX` 完整重建与运行，**3,533 项断言全绿**（F 192、X 180、I 395、M 199、T 381、N 545、S 442、D 387、C 461、G 351），日志见 `logs/KswordARKLightTests-run.txt`
- **十轮独立对抗性评审**（每个模块一个不写实现的 agent，在隔离副本上编译运行并注入变异实测）共查出 **127 条问题、其中 17 条 blocker**，已全部派修。P0 那一轮的代表性问题：
  - I：测试自我印证（把生产归一化输出当"现场"输入，注入静默损坏后 155/155 仍全过）；重定位无法应用时零字节标不可比较，凭空报出 17–18 条假"未解释差异"
  - X：整轮缺席的视图推出 `NoDifferenceObserved`（从没采到推出正常）；见证视图超时被读成"对象已结束"；可信度普查只取首轮
  - F：`ok=true` 但 R0 返回 `NT_ERROR` 时判成"采集成功 + 未发现差异"（DriverDock 走的就是这条）；空账目 `fullyCovered()` 返回 true，等于"忘记填账目 = 100% 完整扫描"
  - M：`mergeReadSpans` 越界读写
- 修复后变异复跑：F 的两条幸存变异各死于 4/2 条断言；X 此前 9 条注入里存活的 7 条**全部被捕获**；I 的关键变异从"155/155 全过"变成 6 条断言失败
- 顺带修掉两个性能问题：JSON 重复键检测 15,343 ms → **39.97 ms**；Cross-view 4000 对象 13,542.7 ms → **43.8 ms**（后者此前让 X-09 的 10 分钟场景根本跑不完）
- 四个受影响的用户态目标全部构建通过：Qt 主程序（`logs/qt-build-driverdock.txt`，0 编译错误）、KswordARKLight、KswordCLI（`logs/light-cli-build.txt`，各 `EXIT=0`）、测试工程。驱动本轮未改动，未重建。
- i18n 审计通过（24289 条源串），语言包各只新增 10 行、无键序重排
- Q-01 变异测试：向生产分支注入一处改动，4 条断言失败，撤回后恢复全绿（`logs/Q-01-变异测试.md`）
- F-11 文案检查：全量检索未发现无证据的安全结论（`logs/F-11-文案检查.md`）
- I-11 源码审计：完整性检查模块只发 4 种只读驱动请求（`logs/I-11-只读扫描审计.md`）

**尚未完成，因此绝大多数编号不是 PASS：**
- **唯一已接入生产 UI 的是 F-05 归一层**：`DriverDock` 的 14 处采集结果展示改走 `describeDriverCollection()`。其余（Cross-view 引擎、PE 归一化核、页表核、区域证据）**都还没有生产调用方**，既有页面仍走各自旧路径。
- **仓库里目前有三处 PE 映射实现**：`KernelCleanImageBaseline::mapPeImage`、`KernelDock.KernelHooks.cpp` 的 `kernelHookReadPeBytesAtRva`（不做重定位）、以及本轮的 `shared/evidence/PeImageMap`。规范要求最终只留一套 —— 本轮交付的是**准备被合并进去的那一套**及其测试，合并动作本身未做，理由见第 6 节。
- 规范第 3.2 节的"UI 能使用 + 保存结果能复核"整体未满足：没有调查会话容器，没有保存/离线重开入口。
- 所有需要"目标环境实测"（E1/E2/E3/E5/E6）的层次本轮**一次都没跑**。
- P1（T/N/S/D）与 P2（C/G）本轮未动。

## 6. 为什么没有把旧 PE 映射直接换掉

`KernelCleanImageBaseline`（2182 行、192 处 Qt 调用、含 WinTrust/签名校验）是内核代码完整性扫描的现役实现，仓库里没有任何测试工程引用它。新核在畸形节上的行为**故意不同**（按节降级为不可比较，而不是整映像 return false），换过去会改变现役安全功能的判定结果。本机没有可用的 R0 验证环境，无法证明替换后的现场行为不退化。

在不能端到端验证的前提下改写一条安全关键路径，属于规范禁止的"猜测通过"。因此本轮把新核和它的 155 项离线断言交付出来，把合并列为**下一步的明确动作**：

1. 先让 `kernelHookReadPeBytesAtRva`（inline hook 的磁盘基线，目前完全不做重定位，其自身 tooltip 已承认这点）改调 `PeImageMap` —— 这条本来就是坏的，换过去只会变好；
2. 再在有 R0 的授权测试机上，用同一批模块对比新旧 `mapPeImage` 的逐字节输出，确认一致后才替换 `KernelCleanImageBaseline`。

逐条状态以 `KSword_Acceptance_Results_Template.json` 为准；那里每条 `PASS` 都有对应的 `executions` 与 `evidence_paths`。
