# KSword 下一阶段验收 · 本地实现映射（F-01）

本文件是 `docs/next/KSword_Next_Roadmap_Acceptance.md` 第 2 节要求的开工核对结果：
对每个验收编号记录**当前工作树中真实存在**的入口、可追踪调用链、现有测试与具体缺口。

生成方式：11 个只读盘点 agent 并行核对源码，再由反驳 agent 对每条“尚未发现实现”和低置信判定重新检索。
路径均为仓库相对路径。分类只有三种：`复用即可` / `需增强` / `尚未发现实现`。


统计：复用即可 5 项，需增强 95 项，尚未发现实现 16 项。


## F · 现有能力复用与证据基础（P0）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| F-01 | 需增强 | docs/next/KSword_Next_Roadmap_Acceptance.md:91<br>docs/next_phase_manifests/memory_evidence.md<br>docs/next_phase_manifests/r3_integration.md | .github/workflows/ci.yml:347/560 分别构建并执行 tests/native/KswordFsDecodeTests/x64/Release/KswordF | 现有 docs/next_phase_manifests 只覆盖 7 个 R0 专题，且只写 R0 协议/handler，不写 UI 入口、R3 入口、测试入口和缺口；缺一份覆盖 F/I/X/M/T/N/S/D/C/G 全部模块的「UI 入口—R3—R0—测试—缺口—归类」映射（本次交付的即是它）。另需在核对表里显式标注 tests/native/KswordARKLightTests/KswordFsDecodeTests 未进 .sln 这一事实，避免本地开发者以为跑 sln 就跑了测试。 |
| F-02 | 复用即可 | Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverClient.h:26<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverTypes.h:12<br>shared/driver/ | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:13 直接 include ../Ksword5.1/Ksword5.1 | 约束当前已满足：grep IOCTL_KSWORD 在 Ksword5.1 UI/Light/CLI 侧只剩注释与文案，实际 DeviceIoControl 旁路（FileDock.cpp:11614、ManualFileSystemParser.cpp、HardwareDock/DiskMonitorStorage*.cpp）全是 FSCTL_/IOCTL_DISK_ 系统码，不打 KswordARK 设备。缺的只是「本轮新增代码不得回退」的守门：没有任何自动检查禁止在 Dock 里出现 IOCTL_KSWORD_ARK_*，也没有 Light 目标的 Qt 依赖断言。建议在 CI 加一条 grep 门禁而不是新增模块。 |
| F-03 | 需增强 | Ksword5.1/Ksword5.1/ksword/process/process.cpp:3457<br>Ksword5.1/Ksword5.1/ksword/process/process.cpp:3463<br>Ksword5.1/Ksword5.1/ksword/process/process.h:546 | 无（KswordARKLightTests 只覆盖 EntityRef 的命令解析，未覆盖身份匹配；ProcessDock/process.cpp 侧无任何单测 | 进程=PID+创建时间、线程=PID+TID+创建时间（process.cpp:3463 BuildThreadIdentityKey）已落地并在 ProcessDock 生效；但驱动/文件/句柄/连接仍只有地址、路径或裸五元组，没有生命周期身份：DriverDock/KernelDock 用 imageBase+名字，句柄用 objectAddress，NetworkDock 用五元组无观察区间。工作树里新出现的 shared/evidence/ObjectIdentity.h（DriverInstanceId/FileIdentity/HandleIdentity/ConnectionIdentity + MatchResult::Candidate）恰好补这一层，但它是 untracked（git status 显示 ?? shared/evidence/），未进任何 vcxproj，grep 全仓零调用方——目前是孤立文件，不能算已接入。 |
| F-04 | 需增强 | shared/driver/KswordArkCallbackMonitorIoctl.h:120<br>KswordARKDriver/src/features/callback/callback_monitor.c:226<br>KswordARKDriver/src/features/debug_output/debug_output_capture.c:75 | 无（callback monitor 的 R3 解析器内联在 IOCTL wrapper 里，无法脱离驱动调用；KswordARKLightTests 无对应用 | 单个协议已有 version/sequence/UTC（KswordArkCallbackMonitorIoctl.h:120 起）与 ETW 归档的 archiveSequence（MonitorDock.h:1014），但全仓没有 collectorId/collectorVersion/sourceGroup/machineId/bootId/captureMode 任何一个字段（grep collectorId/bootId/captureMode 命中 0，只有 Windows 终端 sessionId）；接收时间（R3 收到事件的时刻）不落库；单调时钟只在 KeQueryInterruptTime/QueryPerformanceCounter 局部使用，没有「跨 bootId 禁止相减」的判据。EvidenceSession(KswordARKLight/Ui/EvidenceSession.h:16) 只有 sequence+timestamp100ns+source 字符串，不构成 envelope。工作树新增的 shared/evidence/EvidenceEnvelope.h 的 SourceRef/CaptureWindow/MonotonicComparable 正是缺口所指，但 untracked 且零调用方。 |
| F-05 | 需增强 | Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverTypes.h:63<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverTypes.h:1391<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverTypes.h:1612 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:372「Win32k timer evidence preserves  | 缺一个统一的采集状态枚举：现在 unsupported 是 bool、denied 只体现在 win32Error==ERROR_ACCESS_DENIED、timeout 无独立表达、NotCollected 与「正确的空集合」在 UI 上都是空表格。partial/truncated/unsupported 每个协议自己命名（shared/driver 下 40+ 个 *_STATUS_PARTIAL/_TRUNCATED/_UNSUPPORTED 宏），R3 没有归一层。且采集状态与分析结论未分开：多处直接用「无异常」文案。shared/evidence/EvidenceEnvelope.h:22 的 CollectionStatus/AnalysisConclusion/StatusCarriesObservation 正是这一层，untracked 且零调用方。 |
| F-06 | 需增强 | shared/driver/KswordArkNetworkIoctl.h:380<br>shared/driver/KswordArkNetworkIoctl.h:177<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverAudit.cpp:177 | 无（这些校验都写在 IOCTL wrapper 内部，无脱驱动测试入口） | totalCount/returnedCount 在 ArkDriverTypes.h 里出现 40+ 次，回调监控还带 droppedCount/lostBeforeFirst/OVERFLOW 位，账目基础很扎实；缺的是「总数未知」这一状态——现有协议一律用 totalCount 数字表达，无法区分「总数=0」与「总数不可知」，UI 因此会把 returnedCount 当成完整数量展示。也没有统一的 requested/processed 范围字段（只有网络审计有 budgetRows）。shared/evidence/EvidenceEnvelope.h:69 CoverageAccount(totalKnown 为 OptionalU64、describeRemaining()) 覆盖此缺口，untracked 且零调用方。 |
| F-07 | 需增强 | KswordARKDriver/src/dispatch/ioctl_validation.h<br>KswordARKDriver/src/dispatch/ioctl_validation.c<br>KswordARKDriver/src/dispatch/ioctl_registry.c | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:305「IOCTL decoder rejects malformed  | 校验代码质量高但不可测：所有响应解析都内联在 ArkDriverClient/ArkDriver*.cpp 的 IOCTL wrapper 里，必须有真驱动才能进入，因此「零长度/过短/截断/超大计数/重复游标/游标倒退/未知版本」这 7 类负面输入无法自动覆盖。缺口是把 parse 从 wrapper 里拆成纯函数（可参照 KswordARKLight/Features/Window/Win32kTimerEvidenceModel.h 的做法：输入已采集的 Result 结构，输出行），再进 KswordARKLightTests。另外 shared/driver 里仍有以 FILE_ANY_ACCESS 挂载的破坏性 IOCTL（见 KswordArkCallbackMonitorIoctl.h 用的是 READ\|WRITE，但其它头未统一），本编号变更 R0 时需一并复核。 |
| F-08 | 需增强 | KswordARKLight/Ui/EvidenceSession.cpp:182<br>KswordARKLight/Ui/EvidenceSession.cpp:199<br>Ksword5.1/Ksword5.1/UI/TableInteractionSupport.cpp:701 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:480「evidence JSON schema」只断言 schema  | 三处硬缺口：(1) 没有导入侧——EvidenceSession 只有 export，没有 import/parse，「往返」这一条无从验证；(2) Qt 侧唯一的通用导出是 TableInteractionSupport.cpp:701 的显示文本 TSV，地址已被格式化成 0x... 字符串但没有格式声明字段，且不含底层原值；(3) 现有 JSON 写入一律走 QJsonValue(static_cast<qint64>)（KernelDock.CallbackRuleSerializer.cpp:21 等），读回处用 toVariant().toULongLong()（NetworkDock.FilterAndProcessActions.cpp:1886），对 >2^53 无保护，且 KernelCleanImageBaseline.cpp:1104 的 jsonUInt32 只做到 32 位。shared/evidence/LosslessValue.h（OptionalU64/U64Format::HexAddress/ParseU64）正对这一条，untracked 且零调用方。 |
| F-09 | 需增强 | KswordARKLight/Ui/EvidenceSession.h:36<br>KswordARKLight/Ui/EvidenceSession.cpp:131<br>KswordARKLight/Features/Memory/MemorySnapshot.h:26 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:413「snapshot branch truncates forwar | 「现场刷新不覆盖已保存结果」和「跳转前重验身份」两半都有实现，缺的是持久化那一半：EvidenceSession 是进程内单例（EvidenceSession.cpp:211 GlobalEvidenceSession 的 static），只能导出不能重开；MonitorDock 的 ETW 归档目录按 pid+时间戳新建（MonitorDock.cpp:11160），程序重启后没有任何读回入口（grep loadSession/openSession/重开会话 全仓 0）。所以「保存后结束目标进程、重开旧会话、点击原对象」这条验证目前跑不起来。需要给 EvidenceSession 加一个带 envelope 的落盘+重开路径，并把 ProcessDock.cpp:15566 那套身份复核提成公共函数供离线会话复用。 |
| F-10 | 需增强 | Ksword5.1/Ksword5.1/Framework.h:39<br>Ksword5.1/Ksword5.1/Framework.h:74<br>Ksword5.1/Ksword5.1/ScannerDock/ScannerDock.h:121 | 无（这些都是 QWidget 成员，无脱 UI 测试入口） | 「旧结果不回填」通过 20+ 处各自定义的 generation 计数器已经普遍落地，页面关闭安全靠 kProgress 的 owner 绑定 + QPointer；缺三样：(1) 任务没有统一 id/取消标志/预算/完成状态的抽象——kProgressTask(Framework.h:39) 只有 pid/进度/隐藏位，没有 cancel 与 budget，取消位是每个 Dock 自己造的（cancelFlag / cancelRequested / ticket 三种写法）；(2) 「不可中断的系统调用仍在等待」没有表达位——kProgressTask 无此状态，取消后卡片直接隐藏，会被误读成已清理；(3) 取消可见性无回归测试。建议把 kProgress 扩成任务注册表（id+cancel token+budget+终态），而不是新建任务框架。 |
| F-11 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelSlatIommuAuditTab.cpp:167<br>Ksword5.1/Ksword5.1/KernelDock/KernelSlatIommuAuditTab.cpp:437<br>Ksword5.1/Ksword5.1/DriverDock/DriverDock.Operation.cpp:3302 | 无（文案边界纯人工维护，无自动检查；grep 未见任何断言禁用词的测试） | 文案纪律本身是全仓最强的一块（同源/不等于/无法证明/覆盖范围 等边界句遍布 Kernel/Driver/File/Minidump 页），但全部是硬编码在各页的字符串，没有机器可读的来源分类：不存在 SourceOrigin（LiveKernel / ExternalFile / OfflineSample）字段，也不存在 sourceGroup 用来判断「多视图是否独立来源」——IrpFileSystemParser.h:18 的 layerBypassed 是唯一一处结构化的同源标记，且只覆盖文件枚举一条链路。缺 (1) envelope 里的 origin/sourceGroup 字段，(2) 报告层由这些字段生成信任说明而不是各页手写，(3) 一条禁用词（系统安全/不存在 rootkit）的自动文案检查。shared/evidence/EvidenceEnvelope.h:88 SourceOrigin + BuildTrustStatement 对应此项，untracked 且零调用方。 |
| F-12 | 需增强 | Ksword5.1/Ksword5.1/MainWindow.h:104<br>Ksword5.1/Ksword5.1/MainWindow.h:133<br>Ksword5.1/Ksword5.1/MainWindow.cpp:11039 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp 覆盖 ParseCommandInput 产出的 NavigationT | 路由骨架齐全（Qt 端 10 个具名槽：focusHandleDockByPid(s)/focusMemoryDockByPid/focusNetworkDockByPids/focusWindowDockByPids/focusServiceDockByName/openFileDetailDockByPath/openProcessDetailByPid/ByIdentity；Light 端 EntityRef+8 个 NavigationTarget 的统一路由），懒加载也已解决（ensureDockContentInitialized / pendingNavigation_）。缺三点：(1) 只有 openProcessDetailByIdentity 一条带身份，其余全是裸 PID/裸路径——风险中心 MonitorDock.ArkRiskCenter.cpp:228 就是用 OpenProcessDetailByPid(processId) 跳的，PID 复用后会打开无关进程；(2) 任何一条路由都不带 evidenceId，跳过去看不到「这条结论来自哪批采集」；(3) 目标缺失时只有 ProcessDock 有准确解释（ProcessDock.cpp:15566），其余 focus* 目标不存在时静默无反应。两端路由各一套（Qt 用 invokeMethod 具名槽，Light 用 EntityRef+WM_APP），本轮不宜再造第三套，应把 Qt 槽扩成带 identity+evidenceId 的重载。 |

**本模块最该补的三件事**

1. 【最高性价比】把 shared/evidence/ 从孤立文件变成真接口，或明确废弃它。当前工作树里 shared/evidence/{LosslessValue,EvidenceEnvelope,ObjectIdentity}.{h,cpp} 六个文件已存在且质量对得上 F-03/04/05/06/08/11 的条文，但 git status 显示 ?? shared/evidence/（untracked，本次会话期间新建），全仓 grep 零调用方、零 vcxproj 引用。它现在既不算已实现也不算不存在——这是 F-01「不把没搜到当成不存在」最危险的一处。第一步只需两件事：把它加进 KswordARKLightTests.vcxproj 并补 LosslessValue 的 >2^53/最大地址/空值/零 往返用例（直接满足 F-08 的验证层），再让一个真实调用方（建议 KswordARKLight/Ui/EvidenceSession.cpp 的 exportJson）改用 FormatU64/OptionalU64。不接就删，别让它悬着。

2. 【第二】把响应解析从 IOCTL wrapper 里拆出来，让 F-07 可测。ArkDriverClient/ArkDriverCallbackMonitor.cpp:200-320 那段校验（header.size==expectedResponseBytes、游标不倒退、逐条 sequence 连续、未知 version 拒绝）写得完全够格，但它和 DeviceIoControl 长在同一个函数里，没有驱动就进不去，于是 F-07 要求的零长度/过短/截断/超大计数/重复游标/游标倒退/未知版本七类负面输入一条也跑不了。照 KswordARKLight/Features/Window/Win32kTimerEvidenceModel.h 已经验证过的模式（纯函数吃已采集结构、吐显示行、进 KswordARKLightTests，见 KswordARKLightTests.cpp:372）把 parse 提成 ParseCallbackMonitorResponse(span<const uint8_t>) -> Result，wrapper 只留 I/O。一次改造同时把 F-05/F-06 的状态归一化落到同一个函数里。

3. 【第三】给现有路由补 identity 与 evidenceId，别新建总览页。Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.ArkRiskCenter.cpp:228 现在用 ks::ui::OpenProcessDetailByPid(processId) 裸 PID 跳转，而同一份代码库里 Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.cpp:15534 已经写好了「实时重读创建时间、不符就拒绝并说明 PID 已复用」的完整实现——差的只是调用方传不传创建时间。把 MainWindow.h:104-155 那批 focus*/open* 槽各加一个带 (identity, evidenceId) 的重载，风险中心/驱动页/内存页改调新重载，就能同时满足 F-03 的跨页身份、F-12 的「不跳到首行或相近名字」和 F-09 的「离线跳现场须显式重验」，且不产生第二套路由（Light 侧 KswordARKLight/App/MainWindow.cpp:1223 routeNavigation 已是统一路由，Qt 侧只需对齐语义，不要移植它的 WM_APP 机制）。


## I · 内核完整性检查深化（P0）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| I-01 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:576<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:930<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:1012 | 无（无任何测试工程引用 KernelCleanImageBaseline；tests/native/KswordARKLightTests/KswordARKLightTests.cpp | 身份判据只有 machine/timestamp/SizeOfImage/CheckSum + SigningLevel + loadedModuleBase 匹配（KernelCleanImageBaseline.cpp:1012-1041），缺版本资源与 PDB(RSDS) 身份，且结果是二值——任何一项不合就 return false 整模块拒绝，没有「必要信息不足时降低比较可信度」的分级；ArkRuntimeDynData.cpp:392 readRsdsIdentity / :698 findIdentityMatchedImage 已有可复用的 PDB 身份提取，尚未接进 prepareTrustedImage。 |
| I-02 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:683<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:719<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:1409 | 无（无畸形/截断/重叠节样本用例；仓库内不存在离线 PE 夹具目录） | 文件偏移/RVA/raw-vs-virtual/零填充/SizeOfImage 上限都做了，越界读取有防护；但缺三件：(1) mapPeImage 不检查节区间互相重叠，也不校验 VirtualAddress+VirtualSize <= SizeOfImage；(2) SectionAlignment/FileAlignment 完全没读，raw>virtual 的尾部填充照样进比较；(3) 遇到截断节直接整映像 return false（:759），没有「局部跳过并标注不可比较」的粒度。且同一映射逻辑在 KernelDock.KernelHooks.cpp:594-815 有第二套实现（max(VirtualSize,RawSize) 判归属 + 直接算 raw offset），违反 I 模块「不能再引入第二套 PE 解析器」。 |
| I-03 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:769<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:826<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:864 | 无（无「同一 PE 两个基址 + 改一个非重定位字节」的离线用例） | x64 静态重定位（DIR64/HIGHLOW）与三类动态重定位符号已精确归一化，也没有粗暴忽略整页/整节；但不支持的重定位类型会让 mapPeImage 直接 return false 整模块失败（:895），不满足「不支持的部分标记不可比较」；未解析的动态符号只落成一个全模块布尔 unparsedDynamicRelocations（KernelCleanImageBaseline.h:58），无法定位到 RVA。另有一条真缺口：inline hook 的磁盘基线走 KernelDock.KernelHooks.cpp:594 的 raw 字节路径，完全不应用重定位（自身 tooltip :1439 已承认），基址变化时会把重定位差异报成 hook。 |
| I-04 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.h:60<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:1992<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:2044 | 无 | 「已解释 / 未解释」二分和「不因签名有效就豁免」的原则已落地（updateVerdict 只看 unexplainedRangeCount，从不因 Microsoft 签名放行），但解释规则只有动态重定位一条：没有热补丁依据、没有规则版本号、没有按 RVA 范围粒度的豁免记录。全仓库 grep 不到 hotpatch/热补丁/exemption/规则版本 在完整性路径上的任何实现（KernelDock.CallbackIntercept.cpp:5495 的 ruleVersion 是回调拦截配置，与本模块无关）。需在 KernelCleanImageBaseline.h 的 KernelTextDiffRange::Origin 上扩出 {规则 id, 规则版本, 生效 RVA 范围} 三元组。 |
| I-05 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.h:56<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:1962<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:2008 | 无（无「开头/中部/末尾/跨页改字节」的离线定位用例） | RVA/VA/长度/节名/前后字节/折叠上限（maxRangesPerModule + truncatedRangeCount，:2008）都有，读不到的块只计入 unreadableBytes 而不补 00（:1966，符合「不补 00」）；缺三项硬要求：(1) 差异行没有 readStatus/缺失标记字段，不可读字节在 range 层不可见（整块 continue，KernelCleanImageBaseline.cpp:1959-1968）；(2) 没有 evidenceSource 字段；(3) 完全没接反汇编上下文——KernelTextIntegrityTab.cpp 里 grep 不到 KernelDisassemblyDialog，而现成的 InstructionDecoder 已在 UI/KernelDisassemblyDialog.h:47 可用。折叠后「原始范围可展开」也未实现（截断只计数不保留字节）。 |
| I-06 | 需增强 | KswordARKDriver/src/features/kernel/hook_scan.c:95<br>KswordARKDriver/src/features/kernel/hook_scan.c:319<br>KswordARKDriver/src/features/kernel/hook_scan.c:780 | 无 | 目标所属模块与跨模块判定已有（moduleRangeState + targetModuleBase/targetModuleName），且 kernel_image_section_map.h 已能把地址归到具体节；缺的是「跟随跳转」本身：KswordARKHookDecodeInlineTarget 只解一跳，没有深度上限、字节预算和环检测，也不返回解析路径。导出转发没有独立表示——EAT 侧只是把 RVA 落在导出目录内的项静默标 CLEAN（hook_scan.c:780），SSDT 侧直接 continue 丢弃（ssdt_query.c:805-809），协议里 KSWORD_ARK_KERNEL_HOOK_STATUS_* (KswordArkKernelIoctl.h:435-443) 没有 FORWARDER 状态位，未解析间接目标也和 UNRESOLVED 混在一起。 |
| I-07 | 复用即可 | KswordARKDriver/src/features/kernel/kernel_cpu_integrity.c:938<br>KswordARKDriver/src/features/kernel/kernel_cpu_integrity.c:974<br>KswordARKDriver/src/features/kernel/kernel_cpu_integrity.c:1002 | 无（KswordARKDriver/tests/DriverFunctionalMatrix.ps1 不含 DRIVER_INTEGRITY 用例） | 多处理器组遍历、每行 group/number/vector 标注、IDTR 多数派 + 启动期基线双判据（kernel_idt_consistency.h:15 的 DIVERGED/RELOCATED）都在，MSR 只读 CR0/CR4/EFER/LSTAR/SYSENTER_EIP 这些架构寄存器、无私有 MSR（kernel_cpu_integrity.c:548）。剩余小缺口：状态栏只报 cpuCount 与「响应已截断」（KernelDescriptorTableTab.cpp:374-392），没有逐 CPU 的「已检查/未检查」明细，也没有把「合法差异」（如 IST/每核私有栈导致的 handler 差异）显式排除在异常之外。 |
| I-08 | 需增强 | shared/driver/KswordArkMemoryIoctl.h:266<br>shared/driver/KswordArkMemoryIoctl.h:274<br>shared/driver/KswordArkMemoryIoctl.h:587 | 无 | 三层证据其实都已存在且分层良好：PE 声明（KSW_IMAGE_SECTION_RESULT_EXECUTABLE / kernel_image_section_map.h:22）、页表观测（pteFlags / KERNEL_EXEC_RISK_CODE_PAGE_NOT_EXECUTABLE 与 CODE_PAGE_WRITABLE 明确区分了「PE 说可执行但页表说不是」）、用户可见位（PermissionUser）。缺口有两处：(1) 二级地址转换（EPT/SLAT）权限没有并入这套权限证据——SLAT 视图在 KernelDock 另一个页，未与可执行页行对齐；(2) 这套证据完全在 MemoryDock，KernelDock 的「代码完整性」页（KernelTextIntegrityTab）一个权限列都没有，I 模块的差异行看不到目标页当前权限，无法满足「权限依据明确、不同层不混成同一值」在本模块内的呈现要求。 |
| I-09 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.h:93<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:1877<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:1900 | 无（无「扫描中模块卸载 / 版本变化 / 单页失败」的用例） | 字节级三分类有（scannedBytes / unreadableBytes / differingBytes，KernelCleanImageBaseline.h:93-96），失败模块单独成行且不中断整轮（:1900-1911），取消可断点返回；但缺 I-09 的核心两条：(1) 没有页级统计（尝试/成功/失败/排除各多少页），也没有「排除」这一类（被 moduleFilter 跳过的模块根本不入表）；(2) 读取前后完全不复核模块身份——scanExecutableSections 只在开头 prepare 一次，之后不再确认 base/size/timestamp，模块中途卸载或换版会继续用过期地址解释差异，结果里也没有任何 stale/过期标记（全仓 grep 不到本路径上的 stale 处理）。thread_local 缓存 (:1058) 按 base+size+path 命中，同基址重载会命中旧净映像。 |
| I-10 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:83<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:190<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:343 | 无（无「同字节不同参考来源」的报告对比用例） | 「文件签名状态」与「字节一致」在数据结构上已经分开（CleanImageBaselineResult 的 diskTrustVerified/codeIntegrityTrusted vs differs，KernelCleanImageBaseline.h:16-22），IDT 侧还额外有 profile pack 这条独立参考源；但比较对象只有「本机磁盘」和「内置 profile」两种，且是硬编码的：没有让用户选择参考映像、没有已保存快照作为参考、UI 也不显示「本次比较对象是谁」。全仓 grep 不到 referenceImage / 参考来源 / trustRoot 在本模块的实现。文案上也没有对「本机磁盘不是不可篡改信任根」的显式说明。 |
| I-11 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelDescriptorTableTab.cpp:116<br>Ksword5.1/Ksword5.1/KernelDock/KernelDescriptorTableTab.cpp:769<br>Ksword5.1/Ksword5.1/KernelDock/KernelDescriptorTableTab.cpp:814 | 无（无本模块驱动请求类型审计脚本） | 读取侧确实只读（readVirtualMemory / queryDriverIntegrity / queryImageSignature 全是查询），但 I 模块的三个主力页面里各挂着一条写路径：IDT 页有基线恢复按钮和 mutation=true 的反汇编窗口，inline hook 页有 NOP 摘除，SSDT 页 (KernelDock.Ssdt.cpp:494/568) 也调 patchInlineHook。按 I-11「本模块不发写内存、重定向、卸载或恢复请求」和「定位到地址只导航到现有只读视图」，这三处必须移出本模块或在本模块视图内硬禁用；KernelDescriptorTableTab.cpp:935 的 openKernelAddress 应改为传 mutation=false。另外没有任何脚本/清单记录本模块一次完整查询发出的操作类型，通过条件的「审计所有驱动请求」无证据。 |
| I-12 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelTextIntegrityTab.cpp:250<br>Ksword5.1/Ksword5.1/KernelDock/KernelTextIntegrityTab.cpp:133<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.cpp:432 | 无（无离线差异算法用例，也无实测证据文件；docs/next/KSword_Acceptance_Results_Template.json 中所有条目仍为 N | 真实读取路径齐备且能按模块名过滤到 ntoskrnl / KswordARKDriver，比较覆盖说明有字节级三分类；但两条通过条件都没落地：(1)「一次保存/重开」不存在——本页只有通用 TSV 导出，没有保存完整扫描结果再重新打开的入口（全仓 grep 不到 saveReport/loadReport/重开 在本模块）；(2)「正向差异检测在离线测试映像验证」没有任何离线夹具或测试目标，KswordARKDriver/tests 与 KswordARKLightTests 都不覆盖本模块，因此 R0 可用时也只能给出现场部分，离线部分必然 NOT_RUN。 |

**本模块最该补的三件事**

1. 1) 先切断 I 模块视图里的三条写路径（性价比最高，改动最小、直接决定 I-11 通过与否）：KernelDescriptorTableTab.cpp:116 的「恢复选中 IDT 基线」按钮 + :769/:814 的 restoreIdtBaseline 调用、:935 传给 KernelDisassemblyDialog::openKernelAddress 后被 KernelDisassemblyDialog.cpp:939 强制打开的 setKernelMutationEnabled(true)、以及 KernelDock.KernelHooks.cpp:1834「NOP 摘除选中」→ patchInlineHook（KernelDock.Ssdt.cpp:494/568 同样）。把这些移出完整性视图或在本模块内硬禁用，并补一份「本模块一次完整查询发出的 IOCTL 清单」作为源码审计证据。

2. 2) 把 KernelCleanImageBaseline 立为本模块唯一的 PE 归一化内核，回收 KernelDock.KernelHooks.cpp:594-815 的第二套 raw-RVA 读取（kernelHookReadPeBytesAtRva）。现状是 inline hook 的磁盘基线完全不做重定位，代码自己的 tooltip（KernelDock.KernelHooks.cpp:1439）已经承认这点——这同时踩了 I-03 的通过条件和 I 模块「不能再引入第二套 PE 解析器」的硬约束。顺带把 KernelCleanImageBaseline.cpp:895 遇到不支持的重定位类型就整映像 return false 改成按 RVA 范围标「不可比较」，并在 mapPeImage(:683) 补节重叠 / VirtualAddress+VirtualSize 越界校验。

3. 3) 给 scanExecutableSections（KernelCleanImageBaseline.cpp:1854-2070）补齐「证据完整度」三件套，一次性喂饱 I-05/I-09/I-04：在 KernelTextIntegrityResult / KernelTextDiffRange（KernelCleanImageBaseline.h:56-110）上加页级尝试/成功/失败/排除计数、每条差异的 readStatus 缺失标记与 evidenceSource、以及带规则 id + 规则版本 + 生效 RVA 范围的解释来源（当前 Origin 只有 KnownDynamicRelocation 一条规则）；同时在读前读后各复核一次模块 base/size/timestamp 并给出过期标记（thread_local 缓存 :1058 现在同基址重载会命中旧净映像）。这一步做完再建离线 PE 夹具与测试目标——目前仓库里没有任何测试工程引用 KernelCleanImageBaseline，I-02/I-03/I-05 的「离线自动测试」验证层完全无处执行。


## X · Cross-view 差异解释与复核（P0）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| X-01 | 需增强 | shared/driver/KswordArkProcessIoctl.h:386-389<br>shared/driver/KswordArkProcessIoctl.h:428-432<br>Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.CrossView.cpp:71-77 | tools/driver_functional_ci/driver_test_plan.json:317 用例 process.crossview 与 :399 | 只有 sourceMask 四个位（PUBLIC_WALK/ACTIVE_LIST/CID_TABLE/THREAD_LIST），没有 collector 身份、依赖链与同源组概念。ProcessDock.CrossView.cpp 只显示两列勾选，不显示视图数/来源组数；KernelDockCidTab.cpp:286-289 更是把 queryProcessCrossView、queryThreadCrossView 与 enumCidTable 三个 wrapper 并列成三批行，而 CID 表在驱动侧已经是 crossview 的第三个来源（process_crossview.c:2652 CollectCidTable），属于同一底层 collector 被算成两个来源。shared/evidence/EvidenceEnvelope.h 的 SourceRef.sourceGroup 与 BuildTrustStatement（会输出 viewCount / independentSourceGroupCount / limitationKeys）恰好是 X-01 需要的原语，但这七个文件全部未被 git 跟踪（git status 显示 ?? shared/evidence/*），不在任何 .vcxproj 中，全仓零调用方。 |
| X-02 | 需增强 | shared/driver/KswordArkProcessIoctl.h:489-511<br>Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.cpp:2855-2861<br>Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.cpp:7180-7262 | 仅 tools/driver_functional_ci/driver_test_plan.json:317 process.crossview（gracefu | KSWORD_ARK_PROCESS_CROSSVIEW_ROW 里根本没有 createTime 字段（对比 KswordArkProcessIoctl.h:275/293 的 expectedCreateTime100ns、processCreateTime100ns —— 破坏性 IOCTL 早就用了三元身份，cross-view 却没有）。两处合并（ProcessDock.cpp:7205-7215、ProcessView.cpp:1728-1733）都用裸 PID 当主键，PID 复用会直接误合并；ProcessDock.cpp:7230 更用合成创建时间填洞，会污染下游任何按创建时间做的复核。shared/evidence/ObjectIdentity.cpp:103-124 的 MatchProcessInstance 已经把 bootId+PID+createTime 与 Candidate/Confirmed 语义写完，但文件未跟踪、未编入项目、无人调用。 |
| X-03 | 需增强 | shared/driver/KswordArkThreadIoctl.h:375-402<br>KswordARKDriver/src/features/thread/thread_crossview.c:1742-1840<br>KswordARKDriver/src/features/thread/thread_crossview.c:1264-1350 | tools/driver_functional_ci/driver_test_plan.json:399 thread.crossview（graceful，需 | 所属关系已经比进程侧强：THREAD_ORPHAN 只在 Context->PublicProcessWalkComplete 为真时才判（thread_crossview.c:1800-1806），并且有 THREAD_NOT_IN_PROCESS_LIST 与 DENOISE_POSSIBLE_TERMINATING（thread_crossview.c:955）。但 KSWORD_ARK_THREAD_CROSSVIEW_ROW 没有线程创建时间，也没有所属进程的创建时间，线程只用 processId+processObjectAddress 绑定，进程 PID 复用后残缺所属信息仍会挂到同 PID 新进程；权限不足与线程退出在 R0 都归并成同一个 DENOISE_REFERENCE_FAILURE\|POSSIBLE_TERMINATING 组合（thread_crossview.c:955-956），R3 的 ProcessDock.CrossView.cpp 又完全不渲染 denoiseFlags，界面上三类原因不可区分。ObjectIdentity.cpp:154-158 的 MatchThreadInstance 注释里直接写了 X-03，但同样无人调用。 |
| X-04 | 需增强 | Ksword5.1/Ksword5.1/DriverDock/DriverDock.Evidence.cpp:874-960<br>Ksword5.1/Ksword5.1/DriverDock/DriverDock.Evidence.cpp:1322-1428<br>Ksword5.1/Ksword5.1/DriverDock/DriverDock.h:138-173 | 无。 | 四类对象里只做了「加载模块 -> DriverObject」一条弱关联：collectEvidenceForLoadedModules 用候选名猜 \Driver\Name（DriverDock.Evidence.cpp:906-916 attemptedNamesText），结果只有 driverObjectResolved 布尔和 DriverStart==baseAddress 比对，天然是一对一假设，无法表达「一个模块多个 DriverObject」。DeviceObject 完全不在这条链上（DriverDock.Evidence.cpp 全文无 DeviceObject 字样），设备只在 KernelDeviceDriverObjectsWorker 里做纯 R3 命名空间枚举，两边不交叉。SCM 服务列表与模块列表是两个独立刷新路径、两个独立缓存（m_driverServiceCache / m_loadedModuleCache），从不比对，因此「服务存在未加载」与「模块缺项」无法区分。shared/evidence/ObjectIdentity.h:87-104 的 DriverInstanceId（imagePath+timeDateStamp+pdbSignature）注释里直接标了 X-04，仍未接入。 |
| X-05 | 尚未发现实现 | Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.CrossView.cpp:559-672<br>Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.CrossView.cpp:487-497<br>Ksword5.1/Ksword5.1/KernelDock/KernelDockCidTab.cpp:270-289 | 无。 | 全仓没有「首次缺项 -> 待复核 -> 至少两轮复查 -> 判持续差异」的任何状态机：cross-view 缓存是每次刷新整体覆盖（ProcessDock.CrossView.cpp:620-621），没有 row 级复核计数、复核间隔或观察区间字段；KSWORD_ARK_PROCESS_CROSSVIEW_ROW / KSWORD_ARK_THREAD_CROSSVIEW_ROW 也没有对应字段。仅有的近似物是 DENOISE_POSSIBLE_TERMINATING（KswordArkProcessIoctl.h:423，单轮内的推测，不是复采样）和 EvidenceEnvelope.h:126-141 的 CaptureWindow（未跟踪、未接入）。ProcessDock.cpp:4451-4470 的「采样间隔」控件只服务于进程活动计数刷新，与 cross-view 无关。四种结果（瞬态/持续/对象已结束/无法复查）当前一个都无法产出。 |
| X-06 | 需增强 | KswordARKDriver/src/features/process/process_crossview.c:2762-2812<br>KswordARKDriver/src/features/process/process_crossview.c:2415-2434<br>KswordARKDriver/src/features/process/process_crossview.c:2350-2356 | 无 cross-view 专项测试。同类判据的唯一离线测试在别的实体上：tools/file_handle_scan_fallback_test.cpp（假 D | 能力缺失这一路已经做对（FinalizeRows 的 expectActive/expectCid 会因 MissingCapabilityMask 关掉缺项推断，process_crossview.c:2764-2770；截断置 DENOISE_PARTIAL_EVIDENCE 并把 detailStatus 降级为 PARTIAL，2806-2812）。缺口在「采集失败」这一路：CollectActiveList 在 PsInitialSystemProcess==NULL、指针未对齐、visited 分配失败时（process_crossview.c:2425-2440）只写 Context->LastStatus 就 return，不置 CapabilityMissing 也不置 MissingCapabilityMask，于是 expectActive 仍为 TRUE，整表被打上 MISSING_FROM_ACTIVE_LIST —— 一次来源失败被直接输出成「全系统进程都缺活跃源」。CollectPublicWalk 同理（2350-2356 置了 CapabilityMissing，但 expectPublic 根本不看它）。协议里没有 per-source「本次是否成功采集/覆盖范围」的字段，row 级的 publicWalkStatus/activeListStatus/cidTableStatus 又只在 KernelDockCidTab.cpp 显示，ProcessDock.CrossView.cpp 从头到尾没读过 detailStatus/denoiseFlags。 |
| X-07 | 需增强 | Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.CrossView.cpp:833-929<br>Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.CrossView.cpp:377-410<br>Ksword5.1/Ksword5.1/KernelDock/KernelDockCidTab.cpp:721-790 | 无。 | 「可展开到原始记录」在 KernelDockCidTab 基本做到了（每源 ID + 每源 status + denoise + detailStatus 逐行展开，KernelDockCidTab.cpp:755-790），但 ProcessDock 的主 Cross-View 详情面板（ProcessDock.CrossView.cpp:855-929）只展开 sourceMask/anomalyFlags/offsets/lastStatus/detail，publicProcessId/activeListProcessId/cidTableProcessId、三个 per-source status、detailStatus、denoiseFlags 全部拿到了却没渲染。更缺的是「采样区间」和「复核历史」——这两类数据在协议里根本不存在（依赖 X-05）。无差异对象也无法核对覆盖：anomalyFlags==0 的行只显示「正常」两个字（ProcessDock.CrossView.cpp:87-90），没有覆盖率/来源完整性说明，正是通过条件里点名要避免的红绿图标式结论。 |
| X-08 | 尚未发现实现 | tools/file_handle_scan_fallback_test.cpp:1-40<br>tools/file_handle_scan_fallback_test.cpp:350-400<br>tests/native/KswordARKLightTests/KswordARKLightTests.cpp:27-33 | 无 cross-view 正例。KswordARKLightTests（wmain + Expect，KswordARKLightTests.cpp:27）是仓 | 没有任何「离线快照副本 + 删掉一条已知记录 + 断言指出被删对象与受影响视图」的测试数据或用例。根因是 cross-view 的合并与判定逻辑全部长在 R0（process_crossview.c:2731 / thread_crossview.c:1742）和 UI 线程 lambda（ProcessDock.CrossView.cpp:583-670）里，没有一个可被离线喂数据的纯函数层，因此当前连写这个测试的接缝都没有。可直接照抄的模式是 tools/file_handle_scan_fallback_test.cpp 的假 DriverClient 注入 + 挂进 KswordARKLightTests.vcxproj。 |
| X-09 | 尚未发现实现 | KswordARKDriver/tests/DriverFunctionalMatrix.ps1:1-80<br>tools/driver_functional_ci/driver_test_plan.json:317-330<br>tools/driver_functional_ci/driver_test_plan.json:399-412 | 无 10 分钟高频创建/结束负载。仓内唯一 soak 概念是 HVM 常驻自检（ArkDriverHvm.cpp:96-134，KSWORD_ARK_HVM_C | 没有自有可识别的进程/线程创建-结束负载生成器，也没有「已知活动 + 采集丢失」的账目记录。DriverFunctionalMatrix.ps1 的 crossview 用例是单次 graceful 探针，不会连续采样，更不会对抽样实例做复核。唯一沾边的先例是 KSWORD_ARK_ENUM_HANDLE_STATUS_IS_EXPECTED_CHURN（KswordArkHandleIoctl.h:45）把进程退出竞态标成正常 churn —— 说明「竞态不等于恶意」的判据在句柄链上已有雏形，但 cross-view 链上完全没有对应物（依赖 X-02 的实例身份和 X-05 的复采样）。 |
| X-10 | 需增强 | Ksword5.1/Ksword5.1/ksword/file/file_handle_tools.cpp:1948-2043<br>Ksword5.1/Ksword5.1/ksword/file/file_handle_tools.h:48-60<br>Ksword5.1/Ksword5.1/句柄/HandleDock.cpp:1220-1228 | 句柄：tools/file_handle_scan_fallback_test.cpp（假 DriverClient，覆盖 R0 成功空结果不回退 R3、进程/ | 三个扩展实体的成熟度完全不同，且都没有按 X-10 要求先定义身份/来源语义再接入。句柄最成熟（有 UserOnly/KernelOnly/Both 三态、有截断提示、有 churn 与失败的区分测试），但合并键是 {PID, handleValue}，PID 复用会误配（file_handle_tools.h:220 已有 processCreationTime 字段却没进 key）。网络只做并列展示不做判定：NetworkAuditPage.cpp:1412 那句 diffText 只区分「已对比/未对比」，且来自句柄快照而非连接本身，全文再无 diff 逻辑；ConnectionIdentity 要求的观察区间（ObjectIdentity.h:143-158）不存在。设备完全没有 R0 侧来源，KernelDeviceDriverObjectsWorker 只有 R3 目录枚举。当前也没有任何地方声明「哪些实体已接入统一判定、哪些未接入」的清单，等于用可用页面冒充已支持。 |

**本模块最该补的三件事**

1. 把 shared/evidence/ 接进 cross-view 主链——性价比最高，因为代码已经写好且直接标了 X-01/X-03/X-04。当前 shared/evidence/ObjectIdentity.{h,cpp}、EvidenceEnvelope.{h,cpp} 等 7 个文件在工作树里存在但 git status 显示为未跟踪（??），不在任何 .vcxproj 里，全仓零调用方。落地动作：先把它们加入 KswordARKLightTests.vcxproj 与主工程；再把 Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.cpp:7205-7230 的裸 PID 合并键、KswordARKLight/Features/Process/ProcessView.cpp:1728 的 auditByPid、Ksword5.1/Ksword5.1/ksword/file/file_handle_tools.cpp:1972 的 {PID,handleValue} 键统一换成 ProcessInstanceId::crossSessionKey()，顺手删掉 ProcessDock.cpp:2855 的 KernelOnlyCreationTimeSeed 合成创建时间；最后用 EvidenceEnvelope.cpp:174 的 BuildTrustStatement 在 ProcessDock.CrossView.cpp 状态栏输出「视图数 N / 独立来源组数 M」，并把 KernelDockCidTab.cpp:286-289 里 enumCidTable 与 crossview 的 CID 来源标成同一个 sourceGroup。一次改动覆盖 X-01、X-02、X-03 的主要通过条件。

2. 修 R0 侧「来源采集失败被当成缺项」——这是当前唯一会直接产出假阳性隐藏结论的缺陷。KswordARKDriver/src/features/process/process_crossview.c:2425-2440 中 CollectActiveList 在 PsInitialSystemProcess 为空、head 指针未对齐、visited 集合分配失败时只写 Context->LastStatus 就 return，既不置 CapabilityMissing 也不置 MissingCapabilityMask；而 FinalizeRows（同文件 2764-2770）的 expectActive 只看 MissingCapabilityMask，结果整表被打 MISSING_FROM_ACTIVE_LIST。CollectPublicWalk（2350-2356）与 thread_crossview.c:1773-1780 是同一处病。需要在 KSWORD_ARK_PROCESS_CROSSVIEW_RESPONSE / THREAD 版本里加 per-source 的 collected/failed/覆盖计数字段（shared/driver/KswordArkProcessIoctl.h:513），FinalizeRows 只对「成功且完整」的来源做缺项推断；R3 侧 Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.CrossView.cpp:855-929 把已经解码却从未渲染的 detailStatus/denoiseFlags/publicWalkStatus/activeListStatus/cidTableStatus 显示出来（KernelDockCidTab.cpp:755-790 已有现成渲染代码可抄）。覆盖 X-06 与 X-07 的大半。

3. 补离线 cross-view 回放测试并抽出可测的合并层——X-08 目前零覆盖，且 tools/driver_functional_ci/driver_test_plan.json:317/399 的两条用例 expect 只写了 graceful，等于没有正例。照 tools/file_handle_scan_fallback_test.cpp 的假 DriverClient 注入模式（该文件已证明这套模式在本仓可跑通），把 cross-view 的行合并与异常判定从 ProcessDock.CrossView.cpp:583-670 的 UI lambda 里抽成一个不依赖 Qt 与驱动的纯函数，然后新增一个 CrossViewReplay 测试源文件挂进 tests/native/KswordARKLightTests/KswordARKLightTests.vcxproj:54-65，对进程/线程/驱动三类各准备一份「删掉一条已知记录」的固定输入与预期输出。这个接缝一旦存在，X-05 的复采样状态机也才有地方写和验证。


## M · 内存证据与地址翻译深化（P0）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| M-01 | 需增强 | Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.ProcessRegion.cpp:456<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.ProcessRegion.cpp:471<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.ProcessRegion.cpp:1983 | 无（KswordARKDriver/tests/DriverFunctionalMatrix.ps1 只有 memory.read-phys 一条内存用例，不覆 | RegionEntry (MemoryDock.h:146) 与 RegionSnapshotRow (ProcessRegion.cpp:456) 都只有 base/size/protect/state/type/mappedFilePath，缺 allocationBase、allocationProtect 和「来源」字段；区域表只有 6 列（UiBuild.cpp:585），无来源列。R0 的 KSWORD_ARK_QUERY_VIRTUAL_MEMORY_RESPONSE 已经返回 allocationBase/allocationProtect/queryStatus/source（KswordArkMemoryIoctl.h:443-466），DriverClient::queryVirtualMemory 已封装，但 MemoryDock 全程不调用它，导致 R3 与 R0 两条来源无法并列展示。 |
| M-02 | 需增强 | KswordARKDriver/src/features/process/process_memory.c:649<br>KswordARKDriver/src/features/process/process_memory.c:790<br>KswordARKDriver/src/features/process/process_memory.c:838 | KswordARKDriver/tests/SafeReadRegression.ps1（静态源码回归：KswordARKRuntimeReadMemory 必 | R0 侧已满足分块、IRQL 门禁、部分复制状态与实际字节数（process_memory.c:790-916）。缺口在两头：(1) R3 的 MemoryDock::driverReadMemoryFromUi 恒定传 KSWORD_ARK_MEMORY_READ_FLAG_ZERO_FILL_UNREADABLE，十六进制视图不标注哪些字节是补零的孔洞，只在状态标签里给一个整体 readStatus；DriverDock.Operation.cpp:245 的 zeroFilledChunkCount 记账没有被 MemoryDock 复用。(2) SafeReadRegression.ps1 的断言清单不包含 memory 特性文件，页边界/不可读区域/目标退出没有任何自动用例。 |
| M-03 | 需增强 | KswordARKDriver/src/features/memory/memory_pagetable.c:360<br>KswordARKDriver/src/features/memory/memory_pagetable.c:487<br>KswordARKDriver/src/features/memory/memory_ioctl.c:681 | 无 | 翻译上下文只有 processId：KSWORD_ARK_TRANSLATE_VIRTUAL_ADDRESS_REQUEST (KswordArkMemoryIoctl.h:571) 不带进程实例键或期望创建时间，响应虽有 cr3PhysicalAddress(:619) 却无采集时间戳与分页模式声明字段，进程退出/PID 复用后无法判定上下文失效。R3 的 m_processAttachmentGeneration (MemoryDock.h:1195) 只是丢弃过期 UI 结果的代次计数，不是进程实例身份；现成可复用的 ksword::process::BuildProcessIdentityKey / QueryProcessCreationTimeByPid (process.h:549/674) 未被 MemoryDock 使用。 |
| M-04 | 需增强 | KswordARKDriver/src/features/memory/memory_pagetable.c:87<br>KswordARKDriver/src/features/memory/memory_pagetable.c:393<br>KswordARKDriver/src/features/memory/memory_pagetable.c:529 | 无（DumpPageTable/PageTableWalker 没有单测；驱动侧无页表样本用例） | 层级实现与声明一致、4KiB/2MiB/1GiB 与 canonical 判据都在 memory_pagetable.c:529-700，但缺三样：(1) 没有固定的离线页表样本测试夹具，DumpPageTable.cpp 只能被真实 dump 驱动，无法覆盖缺项/保留位异常；(2) walk 只判 P 位，不检查保留位（MAXPHYADDR 以上位、PS=1 时的保留低位），保留位异常会被当成正常映射；(3) 1GiB 大页在本机无实测标记，KSWORD_ARK_PAGE_TABLE_LARGE_PAGE_1GB 无「未验证」标注路径。 |
| M-05 | 需增强 | KswordARKDriver/src/features/memory/memory_pagetable.c:600<br>KswordARKDriver/src/features/memory/memory_pagetable.c:640<br>shared/driver/KswordArkMemoryIoctl.h:174 | 无 | 「有效硬件映射 vs 无效项」已经区分（不从 not-present 项造 PA），但缺过渡态/软件 PTE 解码：memory_pagetable.c 只看 bit0，没有 Transition(bit11)/Prototype(bit10)/PageFile 编码判读，所有非 present 项一律并成 NOT_PRESENT。另外「先观察后读取」的分时记录缺失——KSWORD_ARK_PAGE_TABLE_ENTRY_INFO 无观测时间戳字段，R3 也不把 QueryWorkingSetEx 观测与后续 readVirtualMemory 打成两个时刻的记录，会被当成一次原子快照。可取之处：evidence 扫描 sampleBytes 默认 0（KswordArkMemoryIoctl.h:431），默认不为凑结果读目标页；exec 扫描的首字节哈希（memory_kernel_exec_scan_helpers.c:609）则会读页，需要在 UI 上标注副作用。 |
| M-06 | 需增强 | KswordARKDriver/src/features/section/section_support.c:36<br>KswordARKDriver/src/features/section/section_support.c:78<br>KswordARKDriver/src/features/section/section_support.c:188 | 无 | profile 门禁只覆盖 DynData 偏移（section_support.c:188/267），但 MMVAD_SHORT / MMVAD 的结构布局是在 section_support.c:36-91 里硬编码的 C 结构体，不随 PDB profile 变化——错版内核下会按错误偏移解读 StartingVpn/ViewLinks 而不是停下来。同时没有进程 VadRoot 的 AVL 遍历（EpVadRoot 只登记不使用），也没有循环/不合理区间的防护与「不支持/数据异常」的显式状态位；R3 侧 MemoryDock 完全不接 Section/VAD，M-06 要求的「保留可用 R3 结果」目前是因为压根没接才成立。 |
| M-07 | 需增强 | Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.KernelExecutableMemory.cpp:148<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.KernelExecutableMemory.cpp:361<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.KernelMemoryEvidence.cpp:182 | 无 | 三处具体缺口：(1) DriverClient 解析丢字段——ArkDriverMemory.cpp:649-668 从未拷贝 rowFlags / backingKind / sectionHintStatus / sectionObjectAddress / controlAreaAddress，ArkDriverTypes.h:1111 的 KernelMemoryEvidenceEntry 里也没有这些成员，于是 R0 已经算好的 RX_PRIVATE / IMAGE_LIKE_MEMORY / Section 归属证据在 UI 上彻底看不到，「未知归属保持未知」无法落地。(2) 进程侧 buildRiskText (ProcessMemoryEvidence.cpp:84) 只有 4 条粗规则，没有 private-executable 与 image 代码差异的分离，也没有起始地址/映射归属不一致规则。(3) ProcessMemoryEvidence.cpp:660 与 ProcessPteTranslate.cpp:491 的 mappedFilePath 两个分支都赋空串，映射归属列恒为空，JIT/RX 区域缺上下文只剩一个「私有执行」标签。磁盘对比复用点在 KernelCleanImageBaseline.cpp:684，但 MemoryDock 明确写着 text diff 留待后续（KernelMemoryEvidence.cpp:532）。 |
| M-08 | 需增强 | Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.ProcessRegion.cpp:1370<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.UiWireAndStatus.cpp:656<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.ViewBreakpointUtil.cpp:38 | 无 | Process→Region→Bytes 一段已经通了（UiWireAndStatus.cpp:656 的区域右键「R0 读取」和 jumpToAddress），但链路缺后半段：全仓库没有 setCurrentWidget(m_tabProcessPteTranslate) 的调用，区域行无法打开该 VA 的翻译详情；更关键的是完全没有调查会话/快照容器——grep 不到任何 InvestigationSession / EvidenceStore / 离线重放入口，各 Tab 只有各自的 m_*Cache，页间共享的唯一「实例」标识是 m_processAttachmentGeneration (MemoryDock.h:1195) 这个进程内计数器，无法跨会话保存，也就谈不上「离线缺项明确显示未保存」。 |
| M-09 | 需增强 | shared/driver/KswordArkMemoryIoctl.h:356<br>shared/driver/KswordArkMemoryIoctl.h:404<br>shared/driver/KswordArkMemoryIoctl.h:416 | 无 | 边界纪律基本到位：ownerKind 有 UNKNOWN 档、RISK_OWNER_MISSING 位、confidence 字段，且不生成调用栈（协议里根本没有栈字段）。缺的是三档语义的显式化——UI 只把 ownerKind 当分类标签打印，confidence 仅在详情文本里裸打一个数字（KernelMemoryEvidence.cpp:403），表格 Owner 列（:743）不区分「直接证据 / 候选 / 未知」；BigPool 的 tag 启发式（TAG_SYSTEM_PTE_LIKE / TAG_MDL_LIKE，KswordArkMemoryIoctl.h:404）与页表硬证据混在同一个 ownerKind 里，多组件共用同一 tag 时无法在 UI 上表达「只是候选」。 |
| M-10 | 需增强 | Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.KernelMemoryEvidence.cpp:474<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.KernelMemoryEvidence.cpp:566<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.KernelMemoryEvidence.cpp:608 | 无 | 范围合法性（逆序/缺范围）与行数、字节上限在协议和 R0 都有（memory_kernel_evidence.c:166，KswordArkMemoryIoctl.h:425-433），进程搜索也有取消（SearchFlow.cpp:644）。缺三项：(1) 无时间预算，任何一条扫描路径都没有 deadline；(2) 内核扫描不可中途取消——KernelMemoryEvidence.cpp:608 的 std::thread 一路跑到 IOCTL 返回，没有 m_scanCancelRequested 那样的取消标志；(3) maxBytes / maxBigPoolRows / sampleBytes 在 UI 上写死为 DEFAULT_* 常量，用户只能改 maxRows，无法按 M-10 要求限定字节/页数预算。 |
| M-11 | 需增强 | Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.DriverMemoryView.cpp:367<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.ProcessRegion.cpp:1711<br>Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.ProcessRegion.cpp:1810 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:404-460（覆盖的是 KswordARKLight 的 Memory | KMDUMP1 已经有可用骨架：declaredRegionSize 与 dumpedBytes 的差值天然表达孔洞，文件头带 pid 和时间戳。但 (1) dumpProcessMemoryToFile 从 lpMinimumApplicationAddress 扫到 lpMaximumApplicationAddress，是「默认保存所有进程内存」，没有用户选区与体积预告；(2) 全仓库只有 ProcessRegion.cpp:1869 一处写 KMDUMP1，没有读取器，离线打不开；(3) DriverMemoryView.cpp:367 的选区快照落盘只有裸字节/十六进制文本，不写来源、读取时间和孔洞位置，而读取时又恒开 ZERO_FILL_UNREADABLE（DriverMemoryRw.cpp:712），补零孔洞在文件里与真实 0 无法区分。DriverDock.Operation.cpp:245 的 zeroFilledChunkCount 是现成的孔洞记账范式，可直接搬。 |
| M-12 | 尚未发现实现 | docs/zh-CN/rxpf.md:374<br>docs/zh-CN/rxpf.md:394<br>KswordARKDriver/tests/DriverFunctionalMatrix.ps1:925 | 无（docs/zh-CN/rxpf.md:374-395 有 !pte 人工核对流程，但对象是 RXPF 的 IDT/别名页，不是 VA 翻译；DriverFunc | 缺一条完整链路：R3 侧需要为自有进程分配一块可控映射（如 VirtualAlloc + MEM_COMMIT + 已知内容）、记录 PID/创建时间/VA/查询时刻，调用 IOCTL_KSWORD_ARK_TRANSLATE_VIRTUAL_ADDRESS 拿到 PA，再用 IOCTL_KSWORD_ARK_READ_PHYSICAL_MEMORY 回读该 PA 比对特征字节，把「对照通过 / 无法稳定核对 / BLOCKED」三态落到记录里。当前 memory_pagetable.c 与 memory_physical.c 两个后端都在，但没有任何调用方把它们串起来做核对，也没有 BLOCKED 语义。 |

**本模块最该补的三件事**

1. 最高性价比：把 R0 已有能力接到 R3，几乎不用写新内核代码。两处断链最刺眼——(a) Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverMemory.cpp:649-668 解析内核证据行时漏拷 rowFlags / backingKind / sectionHintStatus / sectionObjectAddress / controlAreaAddress（ArkDriverTypes.h:1111 的结构体里也没有这些成员），R0 算好的私有 RX、image-like、Section 归属证据在 UI 上全部丢失，补上即可直接推进 M-07 与 M-09；(b) Ksword5.1/Ksword5.1/MemoryDock/MemoryDock.ProcessPteTranslate.cpp:355 的「PTE / VA 翻译」页至今是 VirtualQueryEx+QueryWorkingSetEx，从不调用已注册的 IOCTL_KSWORD_ARK_TRANSLATE_VIRTUAL_ADDRESS（memory_pagetable.c:529 的四级 walk、2MiB/1GiB、canonical、LA57 拒绝全都写好了），接上就同时兑现 M-03 与 M-04 的展示面。顺带把 MemoryDock.ProcessRegion.cpp:456 的 RegionSnapshotRow 补上 allocationBase/allocationProtect/来源，M-01 一并落地。

2. 第二优先：给证据加上「上下文与孔洞」这两个元数据轴。M-03 用现成的 ksword::process::BuildProcessIdentityKey / QueryProcessCreationTimeByPid（Ksword5.1/Ksword5.1/ksword/process/process.h:549/674）替换 MemoryDock.h:1195 那个只防 UI 竞态的 m_processAttachmentGeneration，并在 KSWORD_ARK_TRANSLATE_VIRTUAL_ADDRESS_REQUEST（shared/driver/KswordArkMemoryIoctl.h:571）里加期望创建时间与响应时间戳；M-11 则把 MemoryDock.ProcessRegion.cpp:1841 已有的 KMDUMP1 头（declaredRegionSize vs dumpedBytes 天然是孔洞）改成用户选区触发、补写来源/读取时间，并抄 DriverDock.Operation.cpp:245 的 zeroFilledChunkCount 记账，让 MemoryDock.DriverMemoryView.cpp:367 的落盘不再把补零孔洞混成真实 0。同一改动顺手解决 M-02 R3 侧「缺失字节未标注」的问题。

3. 第三优先：补上目前完全没有的验证与边界代码。M-12 是唯一「尚未发现实现」——需要一条自有进程受控映射 → IOCTL_KSWORD_ARK_TRANSLATE_VIRTUAL_ADDRESS → IOCTL_KSWORD_ARK_READ_PHYSICAL_MEMORY 回读比对的核对链，并引入 BLOCKED 三态。同时给 KswordARKDriver/src/features/memory/memory_pagetable.c 的 walk 加保留位异常判定与过渡态/软件 PTE 解码（M-04/M-05 当前只判 bit0），并把 KswordARKDriver/tests/SafeReadRegression.ps1 的断言清单从 2 个 wrapper 扩到 src/features/memory/*.c 与 process_memory.c——整个 M 模块目前一条自动测试都没有，页边界、逆序范围、错版 profile 全靠人工。


## T · 统一时间线与调查会话（P1）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| T-01 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:12050<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:12683<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:12688 | 无。tests/native/KswordARKLightTests/KswordARKLightTests.cpp 只覆盖 Light 版的 ETW TSV 导出与 DriverLea | 三处采集页都只有“开始/停止/暂停/导出”，完全没有“新建/保存/打开会话”的概念；且“暂停”语义互相矛盾且与后台行为不一致——MonitorDock.cpp:11759 与 ProcessTraceMonitorWidget.Capture.cpp:546 在暂停时于 ETW 回调里直接 return（等于停止采集且静默丢事件），而 KernelCallbackMonitorWidget.cpp:820 只冻结 UI flush、worker 继续读 ring。MonitorDock.cpp:12866 的注释“立即冻结 UI 镜像。待刷新队列完整保留”与实际行为不符。暂停期的缓存/落盘策略在 UI 上无任何呈现（updateStatusLabel MonitorDock.cpp:12758 / ProcessTraceMonitorWidget.cpp:492 只显示计数）。 |
| T-02 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:4246<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:4262<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:4274 | 无 | MonitorDock 的 ETW envelope 已相当完整（provider/eventId/task/opcode/level/keyword/version + 逐属性 offset/hexPreview/fallback + 解析失败时的 rawFallback 原始十六进制尾巴），但：(1) version 只存在于 detailJson 文本里，EtwCapturedEventRow(MonitorDock.h:465) 没有独立 version 字段，归档反序列化(MonitorDock.cpp:200-246)因此无法按版本分流；(2) kEtwArchiveRowVersion(MonitorDock.cpp:113) 是行序列化版本，不是“解析版本/schema 版本”，没有记录用哪一版解码器解出的语义；(3) MonitorDock.cpp:11910 在 fallback 分支也把 decodedReady 置为 true，未解析记录与已解析记录在下游无法区分；(4) ProcessTraceMonitorWidget.Capture.cpp:755-765 的 detail 只带 level/task/opcode/keyword，没有 version、没有原始字节保留。 |
| T-03 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.h:142<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.h:355<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.h:998 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:207-213（“ETW TSV follows visible ord | 数据层已经把“采集过滤(Pre)”与“显示过滤(Post)”分开，UI 也有明确文案（MonitorDock.cpp:6382-6383「前置筛选会主动排除未命中事件；这些事件不会进入全量归档或表格」/「后置筛选只改变显示结果；完整事件仍保留在磁盘归档中」），后置导出还会重扫全量归档而不是只导 UI 窗口。缺的是导出侧的对照面：exportEtwRowsToTsv(MonitorDock.cpp:13337) 的 visibleOnly=false 分支已写好但唯一调用点 MonitorDock.cpp:7204 恒传默认 true，UI 上没有“导出完整会话”按钮，用户无法拿到未经后置过滤的原始会话。ProcessTrace 页更差：只有 UI 过滤（.Actions.cpp:1694），无采集过滤概念，导出（.Export.cpp:285-363）只写 !isRowHidden 的可见行且无任何“被隐藏事件仍存在”的说明。 |
| T-04 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:11773<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:12424<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:13165 | 无 | 源时间被正确保留且不被改写（原始 timestampValue 与派生的时间轴坐标分开，NetworkDock 的 PacketTimelineCaptureSession 也是同一套做法），会话时钟统一为 SystemTime 100ns。但 T-04 要求的其余全部缺失：EtwCapturedEventRow(MonitorDock.h:465) 与 CapturedEventRow(ProcessTraceMonitorWidget.h:170) 都没有接收时间(R3 入队时刻)、精度、时钟校准来源字段；没有任何迟到/乱序/重复时间的识别与标记（全仓 grep 接收时间/receiveTime/arrivalTime/时钟校准/clockCalibration/乱序标记 无命中，只有异步 ticket 防乱序的无关命中）；跨源合并排序根本不存在——ETW 页、进程定向页、内核回调页、网络页各排各的，没有统一 envelope 的归并层，也就谈不上“确定可解释的跨源排序”或对不确定顺序的提示。 |
| T-05 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/ProcessTraceMonitorWidget.h:141<br>Ksword5.1/Ksword5.1/MonitorDock/ProcessTraceMonitorWidget.Runtime.cpp:724<br>Ksword5.1/Ksword5.1/MonitorDock/ProcessTraceMonitorWidget.Runtime.cpp:811 | 无 | 进程实例身份（PID + creationTime100ns）已经在 RuntimeTrackedProcess/TargetProcessEntry 里建模，快照同步路径 ProcessTraceMonitorWidget.Runtime.cpp:811-838 也会在创建时间不匹配时把旧实例标 dead 并重挂新实例——这是可复用的基础。但事件归属路径不用它：buildRelevantEventRow 的 trackedByPid(Capture.cpp:868-880) 只按 PID 查 map，两次快照之间发生的 PID 复用会把新进程事件直接算给旧实例。更关键的是缺“身份不完整临时实体”：Capture.cpp 里没命中 tracked map 且不是创建事件的事件 relevant 保持 false，函数直接 return false 把事件整条丢弃（Capture.cpp:1050 附近），既不建临时实体也不留痕；目标已结束后的迟到事件同样被 isProcessStopEvent 之外的分支丢掉。全仓无“身份不完整/临时实体/instanceKey”一类模型。 |
| T-06 | 需增强 | shared/driver/KswordArkCallbackMonitorIoctl.h:74<br>shared/driver/KswordArkCallbackMonitorIoctl.h:151<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverTypes.h:833 | tools/driver_functional_ci/driver_test_plan.json:2239 "callback.monitor-cycle"（m | 内核回调页是全仓最接近验收要求的一处：源端 droppedCount、游标落后 lostBeforeFirst、R3 队列丢弃三类分开计数且同屏显示，协议层还带 OVERFLOW/MORE_AVAILABLE/SNAPSHOT_RACE 标志。缺口在另外两条链路：(1) ETW 页只有“源丢失 + UI 镜像跳过”两类，解析失败、被前置过滤掉、保留策略删除这三类完全没有计数（MonitorDock.cpp:11895 的解码 fallback 与 11961 的前置过滤都是静默 return）；(2) 进程定向页最差——m_pendingDroppedRows 在 ProcessTraceMonitorWidget.Capture.cpp:583 与 .Actions.cpp:1947 都有累加，但 updateStatusLabel(ProcessTraceMonitorWidget.cpp:492) 从不读它，而 .Actions.cpp:1919 的 `rowCount() - 12000` 表格裁剪连计数都没有，属于典型“把部分轨迹显示为完整”；(3) 没有任何自动测试强制 ring 覆盖或队列溢出，也没有“0 也有可解释统计来源”的呈现。 |
| T-07 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/ProcessTraceMonitorWidget.Runtime.cpp:66<br>Ksword5.1/Ksword5.1/MonitorDock/ProcessTraceMonitorWidget.Runtime.cpp:203<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:908 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:47-51（DriverLeasePolicy 四条断言：OwnsSta | R3 ETW 侧已合格：三个采集页各自用独立 session 名前缀，stopActiveKswordTraceSessionsByPrefix 只停自己前缀的 logger，绝不做全局 stop（MonitorDock.cpp:10948 的“停止选中 ETW 会话”是用户显式操作的独立功能，不在自动路径里）。R0 侧是真缺口：(1) callback ring 只有一个全局 MonitorCategoryMask，callback_monitor.c:319 的 ACTION_STOP 无条件清零，KernelCallbackMonitorWidget.cpp:786 又无条件发这个 STOP——第二个消费者一停就把第一个的采集也关了；协议 KSWORD_ARK_CALLBACK_MONITOR_CONTROL_REQUEST(IoctlHeader:98) 里没有 ownerId/引用计数字段。(2) File Monitor 的 FILE_MONITOR_DRAIN 是破坏性消费（shared/driver/KswordArkFileMonitorIoctl.h:64/78），没有 callback ring 那样的 afterSequence 游标，两个消费者必然互相抢事件。(3) 已经写好并有测试的 KswordARKLight/Core/DriverLeasePolicy.h 只在 Light 版使用，未接入 Ksword5.1 的 ArkDriverClient。 |
| T-08 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:6203<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:6221<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:12033 | 无 | “事件落盘分批、不把整个会话加载成 UI 行对象”这一条已经做到位：ETW 页归档先于 UI 镜像、UI 只保留最近窗口、后置筛选靠 rebuildEtwArchiveFilterAsync 流式重扫磁盘。缺的是磁盘侧的界：全仓没有任何归档大小上限、保留策略或清理逻辑（grep 保留策略/retention/磁盘上限/diskQuota/归档上限/removeRecursively 在 MonitorDock 目录零命中），prepareEtwArchiveSession(MonitorDock.cpp:11143) 每轮在 AppLocalDataLocation/etw_archive 下新建目录，MonitorDock 析构(cpp:4998)也不删，长跑必然把盘写满；到限行为只有一个 m_etwArchiveWriteFailed 布尔（MonitorDock.h:1016）表示“已失败”，不是“按配置停止或淘汰旧数据并明确标记”。采集前 UI 只显示 ETW 缓冲区参数，不显示磁盘上限。ProcessTrace 页 BufferSize/Min/MaxBuffers 在 Runtime.cpp:357-359 硬编码且 UI 不可见。 |
| T-09 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:111<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:120<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:297 | 无 | 版本化本地会话格式、块压缩、流式只读扫描器、legacy 版本兼容全都已经存在且可直接复用，离线读也确实不触发采集。缺三件：(1) 没有任何“打开已保存会话”的 UI 或代码路径——归档目录名 `etw_archive/KswordEtw_<时间>_<applicationPid>`(MonitorDock.cpp:11160) 绑当前进程，scanEtwArchiveFile 只被本进程的 m_etwArchiveClosedSegmentPaths 驱动，关掉程序后这批文件无人能再打开（全仓 grep 打开归档/载入/openArchive/loadArchive/离线重放 零命中）；(2) 归档只存事件行，不存 collector 能力、启用的 Provider 列表、前置筛选规则、丢失统计(m_etwSourceEventsLost/m_etwUiSkippedRows)、查询范围与解析版本，重开后无法对比统计；(3) KswordARKLight/Ui/EvidenceSession.h 的既有证据格式（record/snapshot/exportJson/exportTsv + 隐私脱敏）只在 Light 版，Ksword5.1 完全没有复用。 |
| T-10 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:431<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:457<br>Ksword5.1/Ksword5.1/MonitorDock/MonitorDock.cpp:469 | 无。tests/native/KswordFsDecodeTests/KswordFsDecodeTests.cpp 只覆盖 NTFS runlist 解码的越界/溢出，与归档格式无关。 | “不修改原文件、坏文件有明确诊断”这一半已经做到：整条读路径 mmap 只读、每一类损坏（magic 无效 / 块头越界 / 记录长度无效 / 记录内容不完整 / 尾部不完整 / 行版本不匹配）都有独立错误文本，写失败还会阻断导出。缺的是“只承诺恢复已提交批次”的那一半：(1) scanEtwArchiveFile 一遇错就整体 return false，rebuildEtwArchiveFilterAsync(MonitorDock.cpp:11540-11546) 直接丢弃本次全部结果只显示错误——截断尾部之前已成功解析的行不会被保留，做不到“已提交数据可恢复 + 未提交部分标记缺失”；(2) 磁盘满 / 无权限 / 写失败全部收敛到一个 m_etwArchiveWriteFailed 布尔(MonitorDock.h:1016)，UI 只显示「● ETW归档写入失败」，无法区分原因；(3) 没有“格式版本过新”的分支——MonitorDock.cpp:431-433 只做 `fileVersion != legacy && != current` 的等值判断，未来版本与随机垃圾一律报「格式无效」。 |
| T-11 | 尚未发现实现 ⟨复核：已确认无现成实现⟩ | KswordARKDriver/tests/DriverFunctionalMatrix.ps1:998<br>KswordARKDriver/tests/DriverFunctionalMatrix.ps1:1040<br>KswordARKDriver/tests/DriverFunctionalMatrix.ps1:1048 | 无（DriverFunctionalMatrix.ps1 + driver_test_plan.json 的 file.monitor-cycle / call | 缺一个独立的受控负载辅助程序：按记录顺序启动子进程/线程、加载自有 DLL、写临时文件与 HKCU 测试键、建立 loopback 连接，每类各 100 次带唯一 run id，并输出与采集侧解耦的真值日志；也缺“按 collector 语义预先规定每个操作对应哪些事件”的对账逻辑和“不可解释缺失即失败”的判定。仓库里已有的 runId/临时目录/注册表测试键模式（DriverFunctionalMatrix.ps1:998-1050）和 KswordCLI 的 callback monitor-read --after-sequence（KswordCLI.cpp:1197）可以直接当骨架复用，但负载程序本身与真值日志格式都不存在。 |
| T-12 | 需增强 | Ksword5.1/Ksword5.1/MonitorDock/ProcessTraceMonitorWidget.h:88<br>Ksword5.1/Ksword5.1/MonitorDock/ProcessTraceMonitorWidget.Capture.cpp:884<br>Ksword5.1/Ksword5.1/MonitorDock/ProcessTraceMonitorWidget.Capture.cpp:945 | 无 | 只有三种硬编码字符串关系（根进程 / 子进程(ppid) / 属性关联），没有边类型枚举，缺“相邻发生”“来源直接提供关联”两类；关系只是一列文本，点不开、看不到依据（ProcessTraceMonitorWidget.Export.cpp:85-160 的右键菜单没有关系证据项，openEventDetailViewerForRow(.Export.cpp:243) 只把关系当一行文本打印）。MonitorDock 的 ETW 页 EtwCapturedEventRow(MonitorDock.h:465) 连 relation 字段都没有，只有 parentPid/targetPid 裸字段。全仓无因果措辞控制、无关系边模型（grep 因果/causal/edgeType/关系边/RelationEdge/相邻发生 零命中），因此“不生成由 A 导致 B 的错误链”目前是靠没有这功能来满足的，而不是靠约束满足的。 |

**本模块最该补的三件事**

1. 【1】先统一“暂停”语义并补齐会话状态可见性——性价比最高，一处改动同时解 T-01 与 T-06 的一半。现状是三套页面三种语义：MonitorDock.cpp:11759 与 ProcessTraceMonitorWidget.Capture.cpp:546 在 m_capturePaused 为真时直接在 ETW 回调里 return（暂停 = 静默丢事件，连丢失计数都不加），而 KernelCallbackMonitorWidget.cpp:820 的 setPaused 只冻结 UI flush、worker 仍在读 ring。正确做法是把 MonitorDock/ProcessTrace 改成后者：暂停时继续走 archiveEtwCapturedRow(MonitorDock.cpp:11186) 落盘、只停 m_etwUiUpdateTimer，并把“暂停显示”和“停止采集”拆成两个按钮；同时在 MonitorDock.cpp:12760 和 ProcessTraceMonitorWidget.cpp:492 两处 updateStatusLabel 里显式写出暂停期的缓存/落盘策略。顺带把 ProcessTraceMonitorWidget.Actions.cpp:1919 那个静默 `rowCount() - 12000` 裁剪改成计数并显示（m_pendingDroppedRows 已存在于 ProcessTraceMonitorWidget.h:398 但从未被 updateStatusLabel 读取）。

2. 【2】把已经写好的 ETW 全量归档升级成“可重开的会话”——代价小、覆盖 T-09/T-10/T-03 三项。scanEtwArchiveFile(MonitorDock.cpp:383)、块压缩(MonitorDock/EtwArchiveCompression.cpp)、版本化文件头(MonitorDock.cpp:111-120,297-303) 都已具备，缺的只有三件：(a) 在 prepareEtwArchiveSession(MonitorDock.cpp:11143) 里额外写一份 manifest，记录 collector 能力、启用的 Provider 列表、前置筛选规则、m_etwSourceEventsLost/m_etwUiSkippedRows 统计与时间范围；(b) 在 initializeEtwTab(MonitorDock.cpp:6003) 加“打开会话”入口，直接复用 scanEtwArchiveFile 做离线只读重放（目录名现在带 applicationPid，需去掉进程绑定）；(c) 把 exportEtwRowsToTsv(MonitorDock.cpp:13337) 已支持的 visibleOnly=false 分支接到 UI —— 现在唯一调用点 MonitorDock.cpp:7204 恒传默认 true，用户没有任何办法导出完整会话。

3. 【3】给 R0 回调 ring 加消费者租约，堵住 T-07 唯一的真缺口。callback_monitor.c:319 的 ACTION_STOP 直接 InterlockedExchange(&runtime->MonitorCategoryMask, 0)，任何一个消费者停就把全局 ring 关掉；KernelCallbackMonitorWidget.cpp:786 的 stopCapture 无条件发这个 STOP。判据已经写好且有测试——KswordARKLight/Core/DriverLeasePolicy.h 的 OwnsStartTransition/ShouldStopOnLastRelease，直接移植到 KSWORD_ARK_CALLBACK_MONITOR_CONTROL_REQUEST 协议里加一个 ownerId/引用计数即可。同一批还应给 File Monitor 补非破坏性读路径：shared/driver/KswordArkFileMonitorIoctl.h:64 的 FILE_MONITOR_DRAIN 是消费即删，第二个消费者会被抢走事件，需照 KswordArkCallbackMonitorIoctl.h 的 afterSequence + lostBeforeFirst 模式加一条游标读 IOCTL。


## N · WFP 与网络规则解释（P1）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| N-01 | 需增强 | Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.h:139-193<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:599-698<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:1423-1648 | tools/driver_functional_ci/driver_test_plan.json:1882 "network.wfp-inventory"（ti | 四类对象都枚举到了、GUID/filterId/calloutId/layerId 都在，但关联与关键字段没做：(1) NetworkAuditPage.cpp:1628 filter 的 weight 写成 QStringLiteral("type=%1").arg(filter->weight.type)，展示的是 FWP_DATA_TYPE 枚举而不是权重值，effectiveWeight 在 R3 侧完全没读（R0 侧 network_inventory_wfp.c:230 已正确读 effectiveWeight 并回退 weight，但 R3 只把它塞进 weightText 数字，不区分 weight/effective weight）；(2) filter/callout 行的 providerGuidText/layerGuidText/subLayerGuidText 全是裸 GUID，没有对 provider/sublayer/layer 表做 join，也没有「缺失关联=未知对象」状态；(3) NetworkAuditPage.cpp:1629 action 只输出 type=数字，没映射 BLOCK/PERMIT/CALLOUT_TERMINATING，也没展示 action.calloutKey；(4) R3 与 R0 两路行推进同一个 wfpProviderRows/wfpFilterRows 容器（:1460 与 :1855 起），只靠名字前缀「R0 Provider #N」区分，四张表都没有来源列，也没有按 GUID 去重/对照；(5) 无导出功能（NetworkDock 只有流量筛选配置和 HTTPS CSV 导出）；(6) 截断阈值 :1470/:1523/:1577/:1635（256/256/256/400）静默截断，不上报。 |
| N-02 | 尚未发现实现 ⟨复核：已确认无现成实现⟩ | Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:1630<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.h:174-188<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:1026 | 无 | 全仓没有任何 FWPM_FILTER_CONDITION0 解析代码：条件只显示一个计数字符串。缺 fieldKey→FWPM_CONDITION_* 名称表、FWP_MATCH_TYPE→比较运算符文本、conditionValue 按 FWP_DATA_TYPE 分派（FWP_UINT8/16/32、FWP_V4_ADDR_MASK、FWP_V6_ADDR_MASK、FWP_RANGE0、FWP_BYTE_BLOB 的 AppId、FWP_SID）、IPv4/IPv6 地址族与掩码/前缀渲染、方向条件、以及「未识别条件保留原始字节且不得被当成无条件匹配」的显式标注。建议落成 NetworkDock 下一个可离线单测的纯函数模块（现有 r0EndpointText(NetworkAuditPage.cpp:466) 已有地址族转文本的可复用写法）。 |
| N-03 | 尚未发现实现 ⟨复核：已确认无现成实现⟩ | Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:1585-1640<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:2650-2680<br>KswordARKDriver/src/features/network/network_wfp.c:780-870 | 无 | 没有任何「输入一条连接描述 → 输出匹配/不匹配/信息不足」的分析器：无 layer/sublayer 分组、无 sublayer 内按 weight 排序再跨 sublayer 汇总的仲裁模型、无 callout 动态判定导致的 unknown 保留。zh-CN.json:3901 的知识库词条已经把正确的 [M4] 仲裁模型写清楚了（layer→sublayer→weight→callout），但没有对应代码。需要新建一个只吃「filter 目录 + 连接描述」的纯函数模块，其输入依赖 N-02 的条件解析产物。 |
| N-04 | 需增强 | Ksword5.1/Ksword5.1/NetworkDock/NetworkFirewallPage.h:79-99<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkFirewallPage.cpp:1685-1789<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkFirewallPage.cpp:1797-1864 | tools/driver_functional_ci/driver_test_plan.json:1920 "network.wfp-events"（tier= | 真实运行事件通道是有的（BFE net event 历史+订阅，且带 filterId 反查），符合「只有受支持来源才叫实际命中」的底线；缺的是 N-04 要求的三件事：(1) 事件表没有「来源」列，FwpmNetEventEnum5/4/3 与 Subscribe 与 R0 ALE 三条路混不出区别，FirewallEventEntry(NetworkFirewallPage.h:81-100) 也没有 source 字段；(2) 没有导出，也没有与静态候选并列展示的界面（静态候选本身即 N-03 缺失），所以「两者展示与导出清楚区分」无从谈起；(3) 驱动自有的 ALE 流授权事件（KSWORD_ARK_NETWORK_WFP_EVENT_ROW，带 sequence/dropped/cursorGap 的完整增量协议）只有 CLI 消费，GUI 里看不到；(4) 无离线导入一条运行记录的入口，NetworkDock 全文无 offline/导入样本路径。 |
| N-05 | 需增强 | KswordARKDriver/src/features/network/network_inventory_wfp.c:117-238<br>shared/driver/KswordArkNetworkIoctl.h:175-179<br>shared/driver/KswordArkNetworkIoctl.h:398-418 | 无（driver_test_plan.json 的 network.wfp-inventory 只验协议不验归因） | 协议层已经有 OWNER_UNKNOWN / MODULE_UNKNOWN / PDB_UNAVAILABLE / FIELD_MISSING 四个标志（KswordArkNetworkIoctl.h:175-179），驱动对每行都无条件置上它们，但两端都没兑现：(1) network_inventory_wfp.c 用 displayData.name 冒充 ownerModule（:170/:201/:237），正是 N-05 禁止的「凭名称猜驱动文件」；(2) 结构体里的 classifyAddress/notifyAddress/flowDeleteAddress/ownerImageBase/objectAddress 五个地址字段在 WFP 路径上从未赋值（全仓只有 network_inventory_ndis.c:61 给 objectAddress 赋过值），所以「直接地址证据」一档永远拿不到；(3) R3 只把 flags 打成裸十六进制（NetworkAuditPage.cpp:1843 r0Hex32(entry.flags)），没有解成 直接/候选/未知 三档标签。最省事的补法是把 callback_external_wfp.c 已有的 PE 导出/模块基址解析（:250-400）与 KernelDock.CallbackEnum 的信任级词条搬到 WFP inventory 行上。 |
| N-06 | 需增强 | Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:699-727<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:1652<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:1428-1434 | 无（driver_test_plan.json 只有 expect=graceful 的 probe；无离线模拟 BFE 失败样本） | 部分/截断在 R0 路径上做得很好（partial/truncated/lastStatus 一路到摘要行），但 WFP 目录侧的失败与失效状态有三个洞：(1) NetworkAuditPage.cpp:1652 把 BFE 打不开的错误塞成一条假 Provider 行，会被下游当成一个真实 WFP provider 对象；(2) sublayer/callout/filter 三段枚举失败/ACCESS_DENIED 是静默 return，UI 上表现为「这类对象为 0 个」，无法与真的 0 个区分；(3) 四张 WFP 表都不记录采集时间（NetworkAuditPage 全文无 snapshotTime/采集时间），filterId/calloutId 是可复用的运行时 ID，跨两次刷新拿旧行做联动没有任何失效提示。规则侧的 fingerprint 机制（NetworkFirewallPage.cpp:605/:2121）是现成的样板，可以照搬到 WFP 行（GUID+采集代次）。 |
| N-07 | 需增强 | KswordARKDriver/src/features/network/network_wfp.c:372-392<br>KswordARKDriver/src/features/network/network_wfp.c:1090-1224<br>KswordARKDriver/src/features/network/network_wfp.c:1225-1283 | tools/driver_functional_ci/driver_test_plan.json:2686 "guarded.network.set-rules | 隔离与清理的骨架都在——R0 侧用独占 sublayer GUID（network_wfp.c:372）且 Unregister 只删自有 key（:1225-1264），R3 侧规则名有 KSword 前缀且落库前必须用户确认——缺的是 N-07 要求的验收闭环：(1) R0 规则只覆盖 IPv4 的 ALE_AUTH_CONNECT_V4/RECV_ACCEPT_V4，KSWORD_ARK_NETWORK_RULE(KswordArkNetworkIoctl.h:478) 没有地址字段，无法把测试限定在 loopback 或指定测试进程镜像，只能按 PID/端口/协议；(2) 没有「本工具规则清单」视图：R3 从不查 IOCTL_KSWORD_ARK_NETWORK_QUERY_STATUS 返回的 rules[] 快照，也没有按 KSword 前缀过滤 Windows Firewall 规则的清单页；(3) 退出时不做 KSword 前缀规则的残留清理与清理结果记录（NetworkFirewallPage 析构只停订阅/关 engine）；(4) 没有把候选解释与受支持运行观测对齐的用例——这一条本身还卡在 N-03 未实现上。 |
| N-08 | 需增强 | Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.h:66-93<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:800-850<br>Ksword5.1/Ksword5.1/NetworkDock/NetworkDock.UiBuild.cpp:442-480 | 无 | 连接↔进程的双向跳转已完整且做了 PID 复用防护（NetworkFirewallPage.cpp:4193-4222 用 QueryProcessCreationTimeByPid 前后各读一次再比镜像路径），可直接复用；缺三块：(1) 从 WFP 规则/filter 行回看关联证据没有任何入口——WFP 四张表（NetworkAuditPage.cpp:2610 refreshWfpTables）根本没有右键菜单，只能复制文本；(2) 无「跳到时间线」——NetworkDock 里的 timeline 只是抓包速率时间轴（PacketTimelineRateBucket, NetworkDock.h:280），T 模块的统一调查时间线尚不存在；(3) 无会话保存/离线打开——全仓 grep InvestigationSession/saveSession 零命中，NetworkDock 全文无 offline 路径，唯一可复用件是跨项目的 KswordARKLight/Ui/EvidenceSession.h（带 timestamp100ns、JSON/TSV 导出与 diff），目前 Ksword5.1 主程序未引用。 |

**本模块最该补的三件事**

1. 1) 先补 WFP filter 条件解析与字段修正（一次性拿下 N-02 全部 + N-01 大半，且天然可离线单测）。落点 Ksword5.1/Ksword5.1/NetworkDock/NetworkAuditPage.cpp:1628-1630：目前 weight 写成 filter->weight.type（展示的是 FWP_DATA_TYPE 而不是权重）、action 只有 type=数字、conditions 只有一个计数。做法是新增一个不依赖 Qt 界面的纯函数模块，把 FWPM_FILTER_CONDITION0[] 拆成 fieldKey 名称 / FWP_MATCH_TYPE 运算符 / conditionValue 按 FWP_DATA_TYPE 分派（V4/V6 掩码、RANGE、AppId BYTE_BLOB、SID），未识别条件原样保留并显式标注「不等于无条件匹配」；顺带把 weight 改读 filter->weight 与 filter->effectiveWeight（R0 侧 KswordARKDriver/src/features/network/network_inventory_wfp.c:230 已经是正确写法，照抄即可），action 映射 BLOCK/PERMIT/CALLOUT_TERMINATING 并展示 action.calloutKey。

2. 2) 再做 WFP 对象目录的关联与状态诚实化（覆盖 N-01 的关联要求、N-06 的失败可解释、N-05 的一半）。落点同一文件 NetworkAuditPage.cpp:1423-1652 与 2610-2680：给四张表加「来源」列，停止把 R3(fwpuclnt) 行与 R0(驱动 IOCTL) 行混进同一个 wfpProviderRows/wfpFilterRows 容器（:1460 与 :1855 起两处 push_back）；按 providerKey/subLayerKey/layerKey/calloutKey 建 GUID 索引做 join，关联不上的显式标「未知对象」而不是留裸 GUID；删掉 :1652 那条把 BFE 打开失败伪装成 WfpProviderRow 的假行，改成正式的分区状态；给 sublayer/callout/filter 三段静默 return 的枚举失败（:1477/:1588 附近）补回状态，并把 :1470/:1523/:1577/:1635 的 256/256/256/400 截断上报出来；同时记录一次采集时间与代次，filterId/calloutId 这类可复用运行时 ID 才敢做跨刷新联动。

3. 3) 最后把已经存在但没接到界面的真实运行证据接上，并给归因分级（覆盖 N-04 与 N-05 剩余部分）。落点两处：一是 Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverAudit.cpp:1069 的 queryNetworkWfpEvents——驱动侧 ALE 流授权事件环连 sequence/droppedEventCount/cursorGapCount 的增量协议都写完了，R3 却只有 KswordCLI/ArkDriverExtended.cpp:458 一个调用点，GUI 看不到；把它接进 NetworkFirewallPage 事件页并给 FirewallEventEntry（NetworkFirewallPage.h:81）加 source 字段，区分 FwpmNetEventEnum / Subscribe / R0 ALE 三种来源。二是 KswordARKDriver/src/features/network/network_inventory_wfp.c:170/:201/:237——现在拿 displayData.name 冒充 ownerModule，而结构体里的 classifyAddress/notifyAddress/ownerImageBase 从未赋值；改成用 KswordARKDriver/src/features/callback/callback_external_wfp.c:250-400 已有的模块基址/导出解析填真实地址，R3 再把 OWNER_UNKNOWN/MODULE_UNKNOWN 标志解成 直接/候选/未知 三档（KernelDock.CallbackEnum.cpp:855-869 的信任级词条可直接复用）。


## S · 平台安全状态与原因解释（P1）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| S-01 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:236<br>Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:450<br>Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:591 | tools/driver_functional_ci/driver_test_plan.json:1400 用例 "misc.security-status"（ | 四维只到三维：Qt 侧完全没有“硬件/系统支持”这一维——ApplicationControlPage.cpp:2610-2614 只取了 SecurityServicesConfigured / SecurityServicesRunning / VirtualizationBasedSecurityStatus / RequiredSecurityProperties，唯一能表达硬件支持的 AvailableSecurityProperties 只存在于 KswordARKLight/Features/Misc/MiscFeature.cpp:680（Light 二进制里，且只是字符串拼接）。DeviceGuard 的枚举数组直接以逗号原值落表，没有 1=CG/2=HVCI/3=SBCG 的规范化命名函数，未知枚举值虽然保留原值但也无“未知枚举”标注。VbsPostureTab 的 Severity 四值（Good/Warning/Bad/Neutral）是显示色而非状态模型，没有把“支持/配置/运行/查询结果”做成可离线注入样本的结构体。 |
| S-02 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:129<br>Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:537<br>Ksword5.1/Ksword5.1/MiscDock/ApplicationControlPage.cpp:1871 | 无 | 两条链都缺“采集时间”和“会话回看”。KernelVbsPostureTab.cpp 全文没有 QDateTime/currentDateTime，applySnapshot 只覆盖当前表，重开无历史；ApplicationControlPage::exportCurrentTableTsv (ApplicationControlPage.cpp:1871) 只把可见表格逐格写 TSV，不写来源/时间/build 头。带时间戳的 Ksword::Ui::EvidenceSession（KswordARKLight/Ui/EvidenceSession.h:16，字段 timestamp100ns/source/format）只在 Light 里、且只由导出动作喂入，Qt 主程序无对应物，因此“重启前的历史状态 vs 当前状态并列”没有任何实现。 |
| S-03 | 需增强 | KswordARKDriver/src/features/security_audit/security_audit_query.c:682<br>shared/driver/KswordArkSecurityAuditIoctl.h:108<br>Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:387 | 无（driver_test_plan.json 的 misc.security-status 顺带覆盖 secureBootStatus 的返回，但不做断言） | Secure Boot 端到端已具备且 enabled/capable 已分开，可直接复用；缺的是另外两块：(1) TPM 只有 HardwareOtherDevicesPage.cpp:249 这一处，且 IsEnabled_InitialValue/IsActivated_InitialValue/IsOwned_InitialValue 被 Format-List 拼成文本 blob，没有“存在 / 就绪 / 未就绪 / 查询不可用”的分项状态，也没有独立的启动安全页面。(2) Measured Boot / 测量日志完全没有实现——全仓（排除 .deps）grep "Measured Boot"、"WBCL"、"TCGLog"、"Tbsi"、"Get-Tpm" 零命中，因此目前不存在“查不到日志却输出 Measured Boot 正常”的风险，但也无从满足“每种情况有具体说明”。 |
| S-04 | 需增强 | Ksword5.1/Ksword5.1/MiscDock/ApplicationControlPage.cpp:2437<br>Ksword5.1/Ksword5.1/MiscDock/ApplicationControlPage.cpp:2485<br>Ksword5.1/Ksword5.1/MiscDock/ApplicationControlPage.cpp:368 | tools/driver_functional_ci/driver_test_plan.json:1438 用例 "misc.app-control-statu | “配置 / 执行 / 签名”三者在 Qt 侧没有分开：WDAC 页只报“常见策略文件数 / SIPolicy.p7b 是否存在 / Active 下 .cip 个数”（ApplicationControlPage.cpp:2485），既不区分 audit 与 enforced，也拿不到有效策略。唯一能给 audit/enforced 的公开查询 CodeIntegrityPolicyEnforcementStatus / UsermodeCodeIntegrityPolicyEnforcementStatus 只写在 KswordARKLight/Features/Misc/MiscFeature.cpp:680（Light 二进制），Qt 侧未调用；CiTool 在 ApplicationControlPage.cpp:1440 只用于部署，没有 CiTool -lp 列举有效策略的只读路径。事件日志页确实用文本匹配把 "audit"/"审计"/"would have been blocked" 判成审计（ApplicationControlPage.cpp:368、:2250），但那是事件级判定，不能代表当前策略状态；查不到有效策略时页面显示“未发现常见策略文件”而不是“未知”。 |
| S-05 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:450<br>Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:126<br>Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp:495 | 无 | 冲突逻辑只覆盖一对来源（CI 位 vs 内核模块快照），且做法是“取不利值 + 文字说明”，还没有一个把 N 个来源并排展示的模型。真正需要对照的三方各自孤立：WMI Configured/Running 在 ApplicationControlPage.cpp:2610（Qt）、注册表 DeviceGuard 场景键与 LsaCfgFlags 在 MiscFeature.cpp:702-708（Light）、BCD 在 BootEditorTab.cpp:647（且 BootEditorTab 全文没有读 hypervisorlaunchtype / vsmlaunchtype，grep 零命中），没有任何代码把三者放到一行比较。“待重启/待生效”标注完全缺失：全仓 grep 待重启/需要重启/RebootPending/待生效 在安全姿态相关文件中零命中，也没有构造不一致样本的注入点。 |
| S-06 | 需增强 | Ksword5.1/Ksword5.1/UI/KvmControl.cpp:72<br>Ksword5.1/Ksword5.1/UI/KvmControl.cpp:188<br>Ksword5.1/Ksword5.1/UI/KvmControl.cpp:217 | 无 | 三套原因文本各自为政、无法互相引用：KvmControl.cpp:188（CPU/固件/hypervisor 冲突/AMD 后端缺失）、DisableDseBackend.cpp:591 blockReasonText（驱动未上线 / 构建不支持 / HVCI 开启）、KernelDock.DriverStatus.cpp 的 R0 reasonText（DynData/策略位），没有统一的“能力 → 当前约束 → 来源字段”结构，S 页面也无法反查某个安全配置阻断了哪个能力。另外 DisableDseBackend.cpp:591 起的 BlockReason::HvciEnabled 文案直接指导用户“在 Windows 安全中心 → 内核隔离里关闭内存完整性并重启”，这正是 S-06 通过条件里“不自动建议禁用保护作为默认修复”要排除的写法，需要改成只陈述约束。KernelTextIntegrityTab.cpp:275 已示范用姿态调节结论级别，可作为统一模型的第三个调用方。 |
| S-07 | 需增强 | Ksword5.1/Ksword5.1/MiscDock/DisableDse/DisableDseBackend.cpp:510<br>Ksword5.1/Ksword5.1/MiscDock/DisableDse/DisableDseBackend.cpp:534<br>Ksword5.1/Ksword5.1/MiscDock/DisableDse/DisableDseBackend.h:55 | 无 | 字段级降级只有 DisableDse 一处成型，其余安全姿态页都是整页级：KernelVbsPostureTab::applySnapshot 在 !security.io.ok 时直接 m_table->setRowCount(0) + m_detailEdit->clear()（KernelVbsPostureTab.cpp:202-221），把无需驱动就能读的 CI 位/Secure Boot 一起清空，正是通过条件里禁止的“整页假空”；ApplicationControlPage 的 PowerShell 分支失败只落一条摘要（如 ApplicationControlPage.cpp:2517 的 AppLocker 模块不可用文案），没有把“哪些字段需要管理员、哪些字段普通用户可读”标到字段粒度。仓库没有应用清单要求 requireAdministrator（grep requestedExecutionLevel 零命中），Framework/PrivilegeElevationPrompt.h:53 的提权也是先弹窗询问，符合“无自动提权重启”，但安全姿态页并未接入它做字段级提示。 |
| S-08 | 需增强 | tools/driver_functional_ci/driver_test_plan.json:1399<br>tools/driver_functional_ci/driver_test_plan.json:1423<br>tools/driver_functional_ci/driver_test_plan.json:1438 | tools/driver_functional_ci/driver_test_plan.json 用例 misc.security-status / misc. | 缺“两套配置”这件事本身：driver_test_plan.json 的 misc.* 用例不记录 build 号、VBS/HVCI 是否运行、驱动是否加载，也没有普通配置与 VBS/HVCI 运行配置的分别执行记录；docs/next/KSword_Acceptance_Results_Template.json 的 environments 数组是空模板，仓库里没有任何已填写的实测配置清单（grep 测试环境/配置清单/环境矩阵/已验证配置 只命中规范与模板本身）。驱动不兼容时的 R3 降级只有 Ksword5.1/Ksword5.1/MiscDock/DisableDse/DisableDseBackend.cpp:534 一处，KernelVbsPostureTab 与 ApplicationControlPage 的 R0 分支没有统一的“禁用相关采集并显式降级”开关，无法证明“不因缺某字段阻止其它功能”。 |

**本模块最该补的三件事**

1. 最高性价比：给 KernelVbsPostureTab 补一个 PostureSnapshot 元数据头（采集时间 + build + 每字段 source/rawValue/NTSTATUS），并把失败分支从整表清空改成逐行标注。改动集中在 Ksword5.1/Ksword5.1/KernelDock/KernelVbsPostureTab.cpp 的 applySnapshot(:197)、buildRows(:236)、buildDetail(:537) 三个函数，时间戳与来源模型可直接照抄 KswordARKLight/Ui/EvidenceSession.h:16 的 EvidenceItem{timestamp100ns, source, format}。一次改动同时吃掉 S-02 全部、S-01 的“查询结果”维和 S-07 的“整页假空”硬伤。

2. 第二：把 Win32_DeviceGuard 的 Available/Configured/Running 三元 + CodeIntegrityPolicyEnforcementStatus 抽成一个共享解析函数并给枚举值命名。当前 Ksword5.1/Ksword5.1/MiscDock/ApplicationControlPage.cpp:2610-2614 缺 AvailableSecurityProperties 且不查 CodeIntegrityPolicyEnforcementStatus，而 KswordARKLight/Features/Misc/MiscFeature.cpp:680 两者都有却只做字符串拼接——把 Light 那段查询挪成共享逻辑、加上枚举名映射与“未知枚举保留原值”标注，即可同时补齐 S-01 的硬件支持维和 S-04 的 audit/enforced 区分，无需新驱动能力。

3. 第三：统一“能力不可用原因”模型，以 Ksword5.1/Ksword5.1/UI/KvmControl.cpp:72 的 classify + :188 的 describeAvailability 为模板抽出 {能力, 约束类别, 来源字段, 证据值}，让 Ksword5.1/Ksword5.1/KernelDock/KernelDock.DriverStatus.cpp:1462 的 reasonText 列与 Ksword5.1/Ksword5.1/MiscDock/DisableDse/DisableDseBackend.cpp:591 的 blockReasonText 共用；同时把 blockReasonText 里“去 Windows 安全中心关闭内存完整性并重启”的默认修复建议改成纯约束陈述，否则 S-06 的通过条件直接不成立。


## D · 快照比较与变化解释（P1）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| D-01 | 需增强 | Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:47<br>Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:33<br>Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.cpp:705 | 无 | TableSnapshot (TableSnapshotCompare.h:47) 只有 label / capturedAt / sequence / sourceRowCount / visitedSourceRows / truncatedBy* 五类字段，UI 里露出的只有「快照 %1：保留 %2 行，估算 %3 MiB」(TableInteractionSupport.cpp:1154)；没有 machineId/bootId、collector 版本、选择范围（哪个 Dock、哪次刷新代次、当前筛选条件）与逐来源成功/失败项。shared/evidence/EvidenceEnvelope.h 已把 SourceRef.collectorVersion、CaptureWindow.machineId/bootId/sessionId、CollectionOutcome、CoverageAccount 定义齐了，但它是未跟踪文件（git status: ?? shared/evidence/），全仓零调用点、未进任何 .vcxproj。 |
| D-02 | 需增强 | Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:56<br>Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:135<br>Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:188 | 无 | 引擎侧的语义主键通路已经写完但从未被使用：全仓（含 .cpp/.h/.ui/资源）只有 TableSnapshotCompare.cpp:30 的常量定义、头文件注释和语言包三处出现 kswordSnapshotKeyColumns，没有任何一个 Dock 的表调用 setProperty 申报主键，所以 TableComparisonResult::usesStableKeys (h:135) 恒为 false，比较永远退化成整行文本比较——排序变化虽被 sourceRow/多重集吸收，但任一字段（含地址）变动即产生一对假增删。另外缺两件事：没有「进程实例不得跨启动硬配」的实体类型区分，以及 usesStableKeys 在 TableInteractionSupport.cpp 里从未被读取，用户看不到「本次无法稳定匹配」的不确定标注。 |
| D-03 | 需增强 | Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.h:12<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.h:62<br>Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.h:127 | 无 | 「映像身份 + RVA」的全部素材已存在，但只服务于「内存 vs 磁盘净映像」：CleanImageBaselineResult 里有 moduleBase / preferredImageBase / relativeVirtualAddress / imageSha256 / identityMatched / relocationApplied (KernelCleanImageBaseline.h:12-35)，KernelTextDiffRange 已按 rva 定位差异并区分 KnownDynamicRelocation / Unexplained。快照-快照比较完全按显示文本走，defaultIgnoredColumnKeywords (TableSnapshotCompare.cpp:1043) 只有 序号/行号/时间/日期/Index/No./Sequence/Time/Timestamp/Date，不含地址类关键词，因此两次快照的内核地址列必然产生假差异；也没有「不同版本相同 RVA」「模块缺失」这两种样本的可比性判据。 |
| D-04 | 需增强 | Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:60<br>Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:70<br>Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:103 | 无 | 截断只被记在快照自身并只出现在按钮 tooltip（TableInteractionSupport.cpp:1162「该快照仅覆盖源表前 %1 个扫描行」）和 addSnapshot 的警告文案（cpp:1538-1600）；compare() 里 grep truncat 只命中容量上限（cpp:1134/568/358/370），没有一处根据「新快照被截断 / 来源失败」把旧快照独有行的结论改成不可比较或覆盖不足。更根本的是表格层没有 collector 状态概念：某个 collector 失败时该 Dock 的表就是空表，比较结果直接读作「全部对象已移除」。shared/evidence/EvidenceEnvelope.h 的 CollectionStatus（Unsupported/AccessDenied/Partial）、StatusCarriesObservation()、AnalysisConclusion::NoEvidence、CoverageAccount 正好是这件事的解，但零接线。 |
| D-05 | 需增强 | Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:103<br>Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:123<br>Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.cpp:1103 | 无 | 现状只有「变化的单元格被染色」：TableComparisonModel::data() 的 DisplayRole 分支按 row.displayLater 二选一取值，同一行不会并列旧值与新值，也不携带来源、时间、证据 id 链接（TableComparisonRow 只有 source / earlierRowIndex / laterRowIndex / displayLater / changedSourceColumns，见 TableSnapshotCompare.h:103-115）。TableComparisonResult (h:123) 也没有任何把「风险解释」与「变化事实」分开的字段，UI 侧只有忽略列菜单（TableInteractionSupport.cpp:2237）和截断说明（cpp:1958-1990）。 |
| D-06 | 尚未发现实现 | Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:80<br>KswordARKLight/Ui/EvidenceSession.cpp:182<br>KswordARKLight/Ui/EvidenceSession.cpp:185 | 无（KswordARKLightTests.cpp:479 只断言 exportJson 输出里含 "ksword-arklight-evidence-v1"， | TableSnapshot 纯内存对象，只有 TableSnapshotRetentionLimits（h:80，超限即 evict），全仓没有它的序列化/反序列化函数，也没有任何 .ksession / 调查会话存档格式（grep saveSession/loadSession/InvestigationSession/SessionFile 全仓零命中，只在 docs/next 的验收文档里出现）。EvidenceSession::exportJson (EvidenceSession.cpp:182) 写出了 schema 串 "ksword-arklight-evidence-v1"（cpp:185）但没有对应 reader。因此「旧版 fixture / 未来可选字段保留跳过 / 未来主版本拒绝 / 损坏格式」四种验证用例目前无处落地。 |
| D-07 | 需增强 | KswordARKLight/Ui/EvidenceSession.h:11<br>KswordARKLight/Ui/EvidenceSession.h:34<br>KswordARKLight/Ui/EvidenceSession.cpp:82 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:466-468，断言名 "privacy path redaction" | RedactEvidenceText (EvidenceSession.cpp:82-101) 把所有用户名折叠成同一个字面量 <redacted>，同一次导出里两个不同用户的路径变成同一串——关联被抹平而不是被一致化保留，正好和 D-07「一致映射、关联仍可用」相反。另外只覆盖 C:\Users\ 一种模式：没有 \Device\HarddiskVolume、UNC、%USERPROFILE%、SID、主机名/机器标识；没有「声明哪些内容被删除」的清单字段（EvidenceItem 只有 sequence/timestamp/source/format/text，h:16）；Ksword5.1 主程序侧完全没有脱敏（全仓 grep 脱敏/redact 在 Ksword5.1 只命中 sanitizeTsvField 之类的换行清洗）。 |
| D-08 | 尚未发现实现 | KswordARKDriver/tests/DriverFunctionalMatrix.ps1:554<br>KswordARKDriver/tests/DriverFunctionalMatrix.ps1:620<br>KswordARKDriver/tests/DriverFunctionalMatrix.ps1:735 | KswordARKDriver/tests/DriverFunctionalMatrix.ps1（IOCTL 功能矩阵，非快照前后比对）；KswordARKDr | 现有目标环境脚本只做 IOCTL 覆盖矩阵与安全读回归，harness-owned 对象纪律（driver_test_plan.json 的 policy.tiers/targetGuards）和 per-case cleanup 可直接复用，但没有任何一处做 D-08 要求的流程：预先声明本次要改的字段与对象身份清单 -> 采快照 A -> 只改自有临时服务/规则/测试文件 -> 采快照 B -> 走 TableSnapshotCompareEngine 比对 -> 清理 -> 采快照 C 验证恢复。也没有承载「预期变化清单」的 fixture 文件。 |

**本模块最该补的三件事**

1. 1. 把 shared/evidence/ 接线，让 TableSnapshot 携带一份 EvidenceEnvelope —— 一次改动同时兜住 D-01 与 D-04，性价比最高。具体：把 shared/evidence/EvidenceEnvelope.{h,cpp} 和 LosslessValue.{h,cpp}（目前 git status 显示为未跟踪、零调用点、未进任何 .vcxproj）登记进 Ksword5.1/Ksword5.1/Ksword5.1.vcxproj + .filters；在 Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.h:47 的 TableSnapshot 里加 envelope 字段，由 TableSnapshotCompare.cpp:705 的 capture() 填 machineId/bootId/sessionId/collectorVersion/CollectionStatus/CoverageAccount；再在 TableSnapshotCompare.cpp:1103 的 compare() 里加一条降级分支：任一侧 StatusCarriesObservation() 为假或被截断时，EarlierOnly 行不得渲染成「已删除」，改出「不可比较/覆盖不足」。

2. 2. 让业务表申报语义主键，打通那条写完却从未被调用的 key 列通路 —— 改动小、直接决定 D-02 与 D-03 的噪声水平。具体：在各 Dock 建表处对 QTableView setProperty("kswordSnapshotKeyColumns", ...)（常量在 Ksword5.1/Ksword5.1/UI/TableSnapshotCompare.cpp:30，declaredKeyColumns 读它在 cpp:590），把 TableComparisonResult::usesStableKeys（TableSnapshotCompare.h:135，目前 TableInteractionSupport.cpp 从未读取）显示到比对状态栏作为「无法稳定匹配」的不确定标注；同时给 defaultIgnoredColumnKeywords（TableSnapshotCompare.cpp:1043，现只有 序号/时间/日期 一类）补地址类关键词，并把 Ksword5.1/Ksword5.1/KernelDock/KernelCleanImageBaseline.h:12 已有的 moduleBase/preferredImageBase/relativeVirtualAddress/imageSha256 复用成「映像身份+RVA」比较键。

3. 3. 建 D 模块的离线自动测试（当前该模块零测试，而 D-01~D-07 有六条要求验证层写着「离线自动测试」）。具体：仿 tests/native/KswordARKLightTests/KswordARKLightTests.cpp 的 Expect 风格新增一个只测 TableSnapshotCompareEngine::compare 的用例集——compare() 只吃两个 TableSnapshot、不依赖 QTableView，可直接构造 fixture——覆盖 D-02 的同名异对象与排序变化、D-03 的同映像不同装载基址、D-04 的新快照截断；并顺带补 KswordARKLight/Ui/EvidenceSession.cpp:82 RedactEvidenceText 的一致映射用例（现测试 KswordARKLightTests.cpp:466 只验证了单条路径被抹成同一个 <redacted>）。


## C · 离线崩溃转储分析（P2）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| C-01 | 尚未发现实现 ⟨复核：已确认无现成实现⟩ | Ksword5.1/Ksword5.1/MinidumpDock/MinidumpParser.cpp:435<br>Ksword5.1/Ksword5.1/MinidumpDock/KernelDumpParser.cpp:1655<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpSymbolResolver.cpp:236 | 无 | 全仓库无任何 DbgEng 痕迹：无 dbgeng.dll / DebugCreate / IDebugClient / IDebugControl 引用，无引擎探测路径与版本上报，无独立受控 helper 进程（grep QProcess/CreateProcess 在 MinidumpDock 下零命中）。现状是完全自研的 PAGEDU64/MDMP 解析器 + DbgHelp（DumpSymbolResolver.cpp:250 SymSetOptions/SymInitializeW）在主进程内跑，因此'缺引擎时本页不可用但主程序正常'这条无从验证，'引擎初始化失败'也没有对应错误面。若要落地需新建 helper 可执行工程 + 探测/版本上报接口，并在 MinidumpDock 状态栏区分'引擎缺失'与'解析失败'。 |
| C-02 | 复用即可 | Ksword5.1/Ksword5.1/MinidumpDock/MinidumpParser.cpp:435<br>Ksword5.1/Ksword5.1/MinidumpDock/MinidumpParser.cpp:455<br>Ksword5.1/Ksword5.1/MinidumpDock/MinidumpParser.cpp:473 | 无 | 判据完全按文件签名而非扩展名，空文件（<8 字节）、随机文件、截断文件（'文件过小，无法读取 MDMP 头'/'PAGEDU64 头读取失败'）三条路径都返回 recognized/success 两级状态，用户态 MDMP 走独立 DumpKind::UserMinidump 不会被当成内核 dump，DumpTypeText 覆盖 Full/Kernel/Header/Triage/Bitmap Full/Bitmap Kernel/Automatic，MachineImageTypeText 给出 x64/ARM64/x86。缺口只在验证层：仓库内没有任何 .dmp 样本、没有可离线跑的解析测试入口，无法执行'合法/用户态/空/截断/随机各一次'。 |
| C-03 | 复用即可 | Ksword5.1/Ksword5.1/MinidumpDock/KernelDumpParser.cpp:1662<br>Ksword5.1/Ksword5.1/MinidumpDock/KernelDumpParser.cpp:1690<br>Ksword5.1/Ksword5.1/MinidumpDock/KernelDumpParser.cpp:1707 | 无 | bugcheck code、四参数（DumpBugCheckText.cpp:315 起还有逐参数角色解释）、内核构建号、CPU 架构、崩溃时间、崩溃前运行时长、转储类型、转储写入器状态都已展示；UptimeToText/FileTimeToText 返回空串时该行整条不 push（KernelDumpParser.cpp:1708-1712），满足'缺失不填 0 冒充'。缺口是验证证据：没有与 WinDbg .bugcheck 的对照流程、没有把'字段缺失'显式标注成'转储未记录'（现在是整行消失，读者看不出是缺失还是未实现）。 |
| C-04 | 需增强 | Ksword5.1/Ksword5.1/MinidumpDock/DumpSymbolResolver.h:55<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpSymbolResolver.cpp:236<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpSymbolResolver.cpp:312 | 无 | 已实现的部分很扎实：错版 PDB 会被 ImageMismatch 拦下且 sourceText 返回空串（DumpSymbolResolver.h:51 注释明写'错的行号比没有行号更有害'），无符号时仍保留'模块+0x偏移'，逐模块结论进 symbolStatus 表。缺三块：(1) BuildDefaultSymbolSearchPath(DumpSymbolResolver.cpp:461) 只拼本地目录，既不读 _NT_SYMBOL_PATH 也无任何符号服务器，'网络符号下载须用户启用、可取消、有限时'整条没有落点——可复用的现成代码在 ArkRuntimeDynData.cpp:760（srv*cache*msdl 拼装）与 ThreadStackWindow.cpp:261，但那两处同样没有用户开关/取消/超时；(2) symbolStatus 与 symbolSearchPath 都没写进导出报告（buildReportText 无 [符号] 段）；(3) 无'无网络'与'部分模块无符号'的自动用例。 |
| C-05 | 需增强 | Ksword5.1/Ksword5.1/MinidumpDock/DumpStackWalker.h:1<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpStackWalker.cpp:84<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpStackWalker.cpp:34 | 无 | 现有栈重建是纯栈内存扫描，不是 unwind：全仓库 grep 不到 RtlLookupFunctionEntry / RtlVirtualUnwind / RUNTIME_FUNCTION / StackWalk64 / UNWIND_INFO。StackFrameEntry(MinidumpFormat.h:93) 只有 fromContext 一个可信度位，没有'展开状态'字段、没有帧参数、也没有'到此无法继续展开'的边界帧——UI 与报告只能整体标注'疑似调用栈（栈扫描）'。要满足'展开状态与可用参数'和'无法展开的部分保留边界，不拼接猜测帧'，需要在 DumpStackWalker 之上加一条基于模块 .pdata/UNWIND_INFO 的真实回溯路径，并给 StackFrameEntry 增加 unwindState/stopReason 字段。模块表本身（DumpKernelModuleList）可直接复用。 |
| C-06 | 需增强 | Ksword5.1/Ksword5.1/MinidumpDock/DumpAnalyzer.h:1<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpAnalyzer.cpp:281<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpAnalyzer.cpp:291 | 无 | 三类证据已分开且每条 blame 带 evidence 列表，输出的是整数权重与 High/Medium/Low 而不是责任百分比；ntoskrnl/hal/ntdll 走 IsSystemModule 降权，无地址级证据时 confidence=None 且 headline 写'本转储未能把崩溃地址归属到任何模块'——通过条件的主要部分已满足。缺口：(1) 'Verifier 明确报告'不是独立证据类，0xC4/0xC9/0xE6 只在 DumpBugCheckText 里有文本解释，DumpAnalyzer.cpp 里 grep 不到 0xC4 特判，验证器指名的驱动没有被提升为单独证据；(2) 诊断页把权重画成百分比长度的条形（MinidumpDock.Tables.cpp barPercent），容易被读成'未校准的责任比例'，需要改成分级或去掉数字；(3) 缺'只有 ntoskrnl 栈顶'与'延迟内存损坏'两类样本的文案检查。 |
| C-07 | 需增强 | Ksword5.1/Ksword5.1/MinidumpDock/DumpMemoryReader.h:1<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpMemoryReader.cpp<br>Ksword5.1/Ksword5.1/MinidumpDock/DumpPhysicalMemory.h:1 | 无 | '不靠猜测补全'这一半做得很干净：readPhysical 任何一页缺失就整体失败不做部分填充，翻译失败写 diagnostics（KernelDumpParser.cpp:1834 '无法把内核虚拟地址翻译到物理页'），名称读不到写 '(名称不可用)'/'(名称未捕获)'，TRIAGE 缺 KDDEBUGGER_DATA64 写 '未提供完整 KD 调试数据块，无法解析崩溃现场对象'。缺口是被查看的对象本身：MinidumpDock 下 grep 不到任何 IRP / ERESOURCE / 自旋锁 / 对象查看功能（DumpBugCheckText 里的 IRP 命中全是停止码文案），所以'对缺 IRP/锁/对象页的文件执行对应查看'没有可执行入口；另外缺失措辞没有统一成'转储未包含 / 当前无法解析'两档，散落在各处自由文本里。 |
| C-08 | 需增强 | Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.cpp:356<br>Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.cpp:461<br>Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.cpp:687 | 无 | 已有：解析全程离开 UI 线程、generation 代次淘汰、owner 生命周期互斥、worker 内 try/catch 防 std::terminate、完整转储扫描窗口限死 16 MiB（KernelDumpParser.cpp:56）。完全缺失：MinidumpDock 目录下 grep 不到 cancel / requestInterruption / QFutureWatcher / 超时 / 预算 / QElapsedTimer 任何一项——没有取消按钮、没有时间预算、没有阶段性部分结果、m_parseBusy 期间 beginParse 直接 return 导致用户唯一的手段是等；DbgHelp 关掉了 SYMOPT_DEFERRED_LOADS（DumpSymbolResolver.cpp:248）会同步加载全部 PDB，ApplyPoolTagAttribution 会去磁盘映像里搜标记字节，CollectCrashHistory 查 30 天事件日志，三者都可能长时间阻塞而无法中断；且解析在主进程内，无 helper 可结束，'仅结束本模块拥有的 helper'无对象。 |
| C-09 | 复用即可 | Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.Tables.cpp:362<br>Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.Tables.cpp:543<br>Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.cpp:204 | 无 | 结构上已满足通过条件：MinidumpDock 目录下 grep 不到 QProcess/CreateProcess/ShellExecute/system()/cmd.exe/powershell，没有任何'把转储文本拼成调试器命令'的路径（因为压根没有引擎）；唯一的富文本出口 m_analysisView 全字段 toHtmlEscaped 且禁外链，报告是纯 .txt 无 <img>/外部资源；文件字节访问一律经 DumpFileView 边界检查，MINIDUMP_STRING 有 kMaxStringBytes 上限。缺口只在证据：没有构造含引号/分号/换行/HTML/DML 的模块名与文件名样本跑一次；另外 buildReportText 用 \t 与 \n 分列分行，模块名内含换行会打乱报告结构（不是注入，但会误导重核），建议导出前对字段做一次控制字符替换。 |
| C-10 | 需增强 | Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.cpp:621<br>Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.Tables.cpp:1136<br>Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.Tables.cpp:1390 | 无 | '不自动打包上传'已满足（模块内无网络调用，DumpAutoCheck 只做目录枚举）；从打开到导出的链路是通的。缺三块：(1) 报告头只有'KSword 转储解析报告 / 文件: <路径>'，没有生成时间、工具版本、引擎版本、输入身份（大小/修改时间/哈希，result.fileSize 与 fileLastModifiedUtcMs 已在结构里但没写进报告）、分析范围说明；(2) result.symbolStatus / symbolSearchPath / poolTags / crashHistory 四项只在 UI 页签里，buildReportText 完全没有对应段落（MinidumpDock.Tables.cpp:1136-1400 无 [符号] 段），报告因此不可重核符号可信度；(3) 仓库内零 .dmp 样本、零 fixtures 目录、无样本来源/取得权限说明文档（只有验收文档 E6 一句环境要求），按 C-10 规则本项应先列 BLOCKED。 |

**本模块最该补的三件事**

1. 性价比最高：把导出报告改成可重核件。改 Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.Tables.cpp:1136 buildReportText —— 报告头补生成时间/工具版本/输入身份（result.fileSize + fileLastModifiedUtcMs + 一次 SHA256）/分析范围，并新增 [符号] 段输出 result.symbolSearchPath 与逐模块 symbolStatus（数据已经在 MinidumpFormat.h:296 里躺着，只是没往报告写），顺带把 poolTags/crashHistory 补进去、导出前对字段做控制字符替换。一处函数改动同时推进 C-10、C-04 的报告部分与 C-09 的报告结构安全。

2. 第二：给解析任务补取消与时间预算。现在 Ksword5.1/Ksword5.1/MinidumpDock/MinidumpDock.cpp:356 beginParse 起的 worker 是一条不可中断的直线，DumpSymbolResolver.cpp:519 ApplySymbols（且 SymSetOptions 关了 DEFERRED_LOADS，会同步吃完所有 PDB）、DumpPoolTag.cpp 的磁盘映像搜标记、CrashHistory.cpp 的 30 天事件日志查询任意一段卡住，用户只能等 —— 加一个 std::shared_ptr<std::atomic_bool> 取消令牌 + 分阶段截止时间，让 ParseDumpFile/ApplySymbols/ApplyPoolTagAttribution 在阶段边界检查并返回部分结果，UI 加取消按钮并在状态栏写明'已中断，以下为部分结果'。这是 C-08 从零到有的最小代价，也是 C-01 helper 化之前的过渡。

3. 第三：建离线解析测试入口。仿 tests/native/KswordFsDecodeTests/KswordFsDecodeTests.vcxproj 建一个控制台工程直调 ks::minidump::ParseDumpFile 并断言 recognized/success/kind/bugCheckCode，配上仓库内自造的截断/随机/空/用户态 MDMP 输入与一份有来源说明的真实 x64 small kernel dump —— C-02/C-04/C-08/C-09 的'离线自动测试'验证层现在完全没有落点（KswordCLI 里 grep 不到任何 dump 入口），没有它这四项永远只能停在 NOT_RUN。


## G · 实体关系与跨页调查（P2）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| G-01 | 尚未发现实现 | shared/evidence/ObjectIdentity.h:158-183<br>shared/evidence/EvidenceEnvelope.h:100-172<br>shared/driver/KswordArkDeviceAuditIoctl.h:132-170 | 无。tests/native/KswordARKLightTests/KswordARKLightTests.cpp 只覆盖 ParseCommandInput 的导航语法（wmain  | 全仓没有任何“边/关系”类型：没有 relationType/direction/validity interval/evidenceId/certainty 的结构体，也没有可筛选的关系列表。可复用的原料是三块：未跟踪的 shared/evidence/ObjectIdentity.h 的 ObjectRef{kind,key,strength,evidenceId}、shared/evidence/EvidenceEnvelope.h 的 CaptureWindow(时间)+AnalysisConclusion(确定性)，以及 KSWORD_ARK_DEVICE_AUDIT_ENTRY 已有的 relationDepth/roleHint/confidence。owns/loads/maps/opens/candidate-owner/temporal-neighbor 六类语义在仓库中一个都搜不到（candidateOwner/CandidateOwner/possibleOwner/ownerConfidence/temporal/Neighbor 全部零命中）。 |
| G-02 | 需增强 | Ksword5.1/Ksword5.1/ProcessDock/ProcessDock.cpp:15534-15641<br>Ksword5.1/Ksword5.1/UI/TableInteractionSupport.cpp:2877-2900<br>Ksword5.1/Ksword5.1/UI/TableInteractionSupport.h:100-110 | 无。ProcessDock/TableInteractionSupport 的 identity 拒绝路径没有自动化用例；KswordARKLightTests | 进程实例这一类已经做对（PID+创建时间双校验、拒绝退化为纯 PID），但三处欠账：(1) 覆盖面——Qt 侧仍有 22 处只调 ks::ui::OpenProcessDetailByPid（FileDock.cpp:3819/3885、KernelDockCidTab.cpp:646、KernelDockIpcTab.cpp:478、NetworkAuditPage.cpp:285、NetworkDock.Nids.cpp:406、WindowDock 四个 Tab、句柄/HandleDock.Filter.cpp:1541 等），Light 侧 31 个 NavigationTarget:: 发起点里只有 ProcessView.cpp:2385 和 Handle/HandlePage.cpp:814 两处填 creationTime100ns，EtwMonitorView.cpp:725 明确不填却在状态栏声称“历史 ETW 归属会重新校验”；(2) 启动周期——bootId 只出现在未跟踪的 shared/evidence/EvidenceEnvelope.h，生产代码里零使用，跨重启样本无法区分；(3) 地址复用——驱动/设备/句柄/连接没有任何生命周期身份，KernelDeviceDriverObjectsWorker.h 与 DriverDock 只有裸地址。另外“保留历史对象”没有载体：没有会话存储，历史行退出后只能删。 |
| G-03 | 需增强 | Ksword5.1/Ksword5.1/ProcessDock/ProcessDetailWindow.BaseAndUi.cpp:2307-2331<br>Ksword5.1/Ksword5.1/MainWindow.cpp:10878-10905<br>Ksword5.1/Ksword5.1/MainWindow.cpp:10943-10967 | 无。三条链均无自动化用例；tools/file_detail_tab_latency_test.cpp 与 tools/table_freeze_lifetim | 三条链的“数据”基本齐、“导航”缺口明确：链一可用但只按裸 PID 且缺 Thread/Module 的独立 identity；链二在 KSWORD_ARK_DEVICE_AUDIT_ENTRY 里已经同时拿到 driverName/serviceName/imagePath（KswordArkDeviceAuditIoctl.h:155-170），但 HardwareDock 的设备栈表没有任何右键跳转，MainWindow::focusServiceDockByName / openFileDetailDockByPath 现在只被 StartupDock.Actions.cpp:495 和 ServiceDock.Actions.cpp:1008 调用，Device→Service/Image 这一跳完全没接；链三缺 Connection→Timeline。“无数据环节明确缺失”也没落实：ServiceDock::focusServiceByName (ServiceDock.cpp:50-82) 找不到目标时只是静默 requestAsyncRefresh，不解释未命中。整条链无法“保存会话”，因为没有会话持久化。 |
| G-04 | 尚未发现实现 | Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverClient.h:316-341<br>shared/driver/KswordArkDeviceAuditIoctl.h:106-115<br>shared/driver/KswordArkDeviceAuditIoctl.h:91-94 | 无。没有 10,000 节点/50,000 边数据集，也没有第 7.2 节 L4 的性能基准脚本；tools/ 下唯一的延迟骨架 file_detail_tab | 没有图/关系视图，也就没有“初始一跳、200 节点/500 边上限、显式继续展开”。可直接照抄的既有模式有三处：ArkDriverClient 的 maxNodes 预算 + totalCount/returnedCount 分离、KswordArkDeviceAuditIoctl.h 的 DEFAULT/HARD_MAX_ROWS 双上限 + RESPONSE_FLAG_TRUNCATED、GlobalUiSearch.cpp 的 searchGeneration 分片异步扫描（processNextSearchChunk + singleShot(0) 让出事件循环 + 进度行）。未跟踪的 shared/evidence/ScanBudget.h 已经写好 ScanBudget/EvaluateBudget/ApplyStopToCoverage，但没有任何调用方。 |
| G-05 | 尚未发现实现 | Ksword5.1/Ksword5.1/UI/ThemeStatusRole.h:27-45<br>shared/driver/KswordArkDeviceAuditIoctl.h:60-66<br>shared/driver/KswordArkDeviceAuditIoctl.h:96-106 | 无。四类节点（缺 owner / 已卸载 / 来源未采集 / 与实际不一致）没有任何离线用例。 | 没有实体节点/关系模型，四类语义无处安放。词汇层面已有可复用底子：ThemeStatusRole 的 Idle/Info/Success/Warning/Error（明确写了“Warning=降级可用、部分失败、结果不完整”），DeviceAudit 的 STATUS_UNAVAILABLE/NOT_FOUND/QUERY_FAILED/UNSUPPORTED 与 RISK_NAME_MISSING，以及未跟踪的 CollectionStatus{NotCollected,Success,Partial,Unsupported,AccessDenied,Timeout,Error} + AnalysisConclusion{NoEvidence,NoDifferenceObserved,...}（EvidenceEnvelope.h:20-70，含 StatusCarriesObservation 判据）。需要把这套映射到节点，并保证“无边”不进入任何风险打分——目前无从验证，因为没有打分器也没有节点。 |
| G-06 | 尚未发现实现 | KswordARKLight/Ui/EvidenceSession.h:16-56<br>KswordARKLight/Ui/EvidenceSession.cpp:182-211<br>KswordARKLight/App/MainWindow.cpp:1315-1360 | 无。EvidenceSession 未出现在 KswordARKLightTests.cpp 的 Expect 断言里（只 #include 了头文件）；没有导 | 三件事都缺：(1) 没有会话文件格式与 load 路径——grep saveSession/loadSession/SessionFile/importJson/importSession 全仓零命中；(2) 导出里没有实体 id / 证据 id——Light 的 EvidenceItem 只有 sequence/timestamp/source/format/text，Qt 的导出走 TableInteractionSupport 的可见行 TSV，都是显示文本；(3) 因此“图/列表/详情/导出引用同一 id”无从谈起。未跟踪的 shared/evidence/EvidenceJson.h 已经把无损 JSON（禁浮点、64 位写字符串、null≠0≠""、深度上限）和 EvidenceEnvelope.evidenceId 写好，但没有任何 vcxproj 引用它，也没有把 EvidenceSession 迁过去。 |
| G-07 | 需增强 | Ksword5.1/Ksword5.1/UI/TableInteractionSupport.h:20-95<br>Ksword5.1/Ksword5.1/UI/TableHeaderSortingSupport.h<br>Ksword5.1/Ksword5.1/UI/GlobalUiSearch.cpp:1-2008 | 无（针对本编号）。tools/table_freeze_lifetime_test.cpp 与 tools/file_detail_tab_latency_te | “沿用现有体系”那半边可以直接复用（排序 = NumericSortRole + TableHeaderSortingSupport，键盘 = GlobalUiSearch 的 Up/Down/Enter/Esc + TableInteractionSupport 的 Ctrl+C，不靠颜色 = ThemeStatusRole，主题/语言 = theme.h + LanguageManager + 两个 audit 脚本）。缺的是本编号的交付物本身：(1) 没有关系列表控件；(2) 没有跨页“返回”——全仓只有 FileDock/RegistryDock 的路径级前进后退（RegistryDock.cpp:290-305），MainWindow 的 focus*/open* 槽是单向的，跳过去就回不来；(3) 地址完整复制在 Light 侧靠 VirtualListView 的行文本，Qt 侧靠 TSV 导出，关系视图需要各自补一遍。 |
| G-08 | 尚未发现实现 | shared/evidence/EvidenceEnvelope.h:55-70<br>shared/evidence/EvidenceEnvelope.h:150-172<br>Ksword5.1/Ksword5.1/DriverDock/DriverDock.Evidence.cpp:83-100 | 无。没有“换布局/换排序后导出结论不变”的对照用例。 | 没有关系图，本编号的四项（布局无关、节点大小/颜色不携带未定义风险含义、推断可展开到规则与来源、换布局导出结论不变）全部无处可验。已有可复用的“克制表达”样板：DriverDock.Evidence 的 differenceClass 分类文案、KernelKnowledgeCatalog 的“只读观察路径/证据不能证明什么”结构，以及未跟踪的 AnalysisConclusion（NoEvidence 明确不是“正常”）+ BuildTrustStatement（只产出覆盖与限制事实，注释里禁止输出“系统安全/不存在 rootkit”）。落图时必须把这三样当成硬约束而不是文案。 |

**本模块最该补的三件事**

1. 【重要前提，先说清楚】shared/evidence/ 下 12 个文件（ObjectIdentity/EvidenceEnvelope/LiveNavigation/ScanBudget/EvidenceJson/LosslessValue，共 2507 行）在当前工作树里**存在但完全未跟踪**（git status 显示 `?? shared/evidence/`，git log 无记录），且没有任何 .vcxproj/.filters/源码引用它们，没有调用方也没有测试——它是并行会话刚落下的地基，随时可能被别人的 stash/清理卷走。第一件事就是把它接进 Ksword5.1.vcxproj / KswordARKLight.vcxproj / KswordARKLightTests.vcxproj（含 .filters）并提交，让 Ksword::Evidence::ObjectRef + NavigationRequest + LiveNavigationDecision 成为唯一的跨页导航契约。不接进构建，G-01/G-02/G-05/G-06 全部只能从零重写一遍。

2. 【性价比最高的一段改动】把 Ksword5.1/Ksword5.1/UI/TableInteractionSupport.cpp:2877 的 OpenProcessDetailByIdentity 变成默认路径。现在只有 MonitorDock/DirectKernelCallMonitorWidget.cpp:2244 一处用它（并在缺 creationTime 时禁用菜单项 + 给出理由），另有 22 处仍调裸 PID 的 OpenProcessDetailByPid：FileDock.cpp:3819/3885、KernelDockCidTab.cpp:646、KernelDockIpcTab.cpp:478、NetworkAuditPage.cpp:285、NetworkDock.Nids.cpp:406、NetworkDock.InternalHelpers.cpp:109、WindowDock 四个 Tab、句柄/HandleDock.Filter.cpp:1541、MonitorDock.ArkRiskCenter.cpp:228 等；Light 侧 31 个 NavigationTarget:: 发起点里只有 ProcessView.cpp:2385 和 HandlePage.cpp:814 填了 creationTime100ns，EtwMonitorView.cpp:725 不填却在状态栏声称会重新校验。拒绝逻辑（ProcessDock.cpp:15534-15641）已经写好且写对，缺的只是把 identity 一路带到每个右键菜单——这是 G-02 通过条件的直接欠账，改动集中在既有菜单代码，不需要新架构。

3. 【最省力的一条链】在 Ksword5.1/Ksword5.1/HardwareDock/HardwareDock.cpp 的设备栈表上补右键跳转：KSWORD_ARK_DEVICE_AUDIT_ENTRY 已经同时返回 driverName/serviceName/imagePath/driverObjectAddress/deviceObjectAddress/relationDepth/confidence（shared/driver/KswordArkDeviceAuditIoctl.h:132-170），而 MainWindow::focusServiceDockByName 与 openFileDetailDockByPath 早就存在、只被 StartupDock.Actions.cpp:495 与 ServiceDock.Actions.cpp:1008 各用了一次。把这两个槽接到设备栈行上，G-03 的第二条链（Device→DriverObject→Driver image→Service）当天就能走通，数据一行都不用新采。同时必须修 ServiceDock::focusServiceByName（ServiceDock.cpp:50-82）：找不到目标时它只是静默 requestAsyncRefresh，不解释未命中，违反 G-03 的“无数据环节明确缺失”和 F-12 的“目标缺失时准确解释”。


## Q · 集成、可靠性与交付（贯穿 P0–P2）

| 编号 | 判定 | 现有入口（节选） | 现有测试 | 缺口 |
|---|---|---|---|---|
| Q-01 | 需增强 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:36<br>tests/native/KswordARKLightTests/KswordARKLightTests.vcxproj:54<br>tests/native/KswordARKLightTests/KswordARKLightTests.vcxproj:55 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp（单 wmain，断言以 label 命名："new driver sta | 两个测试工程确实链接生产 .cpp（符合“不得复制实现到测试”），但覆盖面极小：Ksword5.1 主程序 531 个 .cpp 只有 FileDock/NtfsRunListDecode.cpp 一个被 KswordFsDecodeTests.vcxproj:55 编入；KswordARKDriver/src 下 246 个 .c 没有任何单元测试。唯一的“驱动测试” KswordARKDriver/tests/SafeReadRegression.ps1 用 Get-CFunctionBody + Assert-Matches 做源码正则匹配，正是 Q-01 判为不算通过的 grep 式验证。tools/scanner_tests/scanner_self_test.cpp、tools/file_detail_tab_latency_test.cpp、tools/table_freeze_lifetime_test.cpp、tools/file_handle_scan_fallback_test.cpp 四个 harness 不在任何 .vcxproj/.filters/CI 里（全仓 grep 文件名只命中 docs/zh-CN/rxpf.md 提到的另一个 rxpf_vm_test），从未被编译执行。 |
| Q-02 | 需增强 | tests/native/KswordFsDecodeTests/KswordFsDecodeTests.cpp:139<br>tests/native/KswordFsDecodeTests/KswordFsDecodeTests.cpp:73<br>tools/scanner_tests/scanner_self_test.cpp:596 | KswordFsDecodeTests（reject: null pointers / width over 8 rejected / 越界与 INT64_MI | 缺三样：(1) 没有任何“验收编号 ↔ 测试”映射产物，docs/next/KSword_Acceptance_Results_Template.json 的 evidence_paths 全为空数组，无人填；(2) 取消用例只有 driver_test_plan.json:16 的 timeout 一条口径，Qt/Light 侧全无 —— 全仓 isInterruptionRequested/m_cancelRequested 只有 4 处实际调用（FileDock.cpp、ksword/file/file_handle_tools.cpp 等），且无对应测试；(3) 竞态/过期用例只有 KswordARKLightTests 的 snapshot 分支，Ksword5.1 无测试工程因此完全靠人工。 |
| Q-03 | 需增强 | .github/workflows/ci.yml:23<br>.github/workflows/ci.yml:294<br>.github/workflows/ci.yml:367 | .github/workflows/ci.yml:355-365（构建后立即运行 KswordFsDecodeTests.exe，非零退出即 throw）；:5 | 三个具体缺口：(1) shared/evidence/{EvidenceEnvelope,LosslessValue}.{h,cpp} 已存在于工作树（git status 显示未跟踪）却没有进任何 .vcxproj/.filters，且全仓 grep EvidenceEnvelope/LosslessValue 除自身外零引用 —— 新增源文件未纳入项目；(2) KswordARKLightTests.vcxproj 与 KswordFsDecodeTests.vcxproj 都不在 Ksword5.1/Ksword5.1.sln 的 Project 列表里（sln 只有 11 个工程），本机 msbuild sln 不会构建也不会运行测试，只有 CI 里显式 msbuild 才跑；(3) KswordARKDriver.vcxproj 里 SignMode=Off（:551-566），没有 InfVerif/ApiValidator/RunCodeAnalysis 开关，AGENTS.md:53 记录的 WDK 后置阶段卡死属于人工处置，CI 无“驱动后置校验失败即失败”的机器判据。 |
| Q-04 | 需增强 | shared/driver/KswordArkCapabilityIoctl.h:14<br>shared/driver/KswordArkCapabilityIoctl.h:15<br>shared/driver/KswordArkCapabilityIoctl.h:26 | 无（KswordARKLightTests 与 KswordFsDecodeTests 都不覆盖 ArkDriverClient；无旧协议样本 fixture） | 降级基础设施有一半：能力矩阵协议（feature state UNKNOWN/UNAVAILABLE/DEGRADED/DENIED_BY_POLICY，KswordArkCapabilityIoctl.h:47-51）、无驱动识别（ArkDriverClient.cpp:84）都在，但版本兼容没做：ArkDriverCapability.cpp:63 对 version 做严格相等，旧驱动一律被判成 "driver capability response header invalid" 而不是降级；全部 49 个 shared/driver/*Ioctl.h 里只有 KswordArkCallbackIoctl.h:122-123 保留了 V2/V3 双版本常量作为先例，其余都是单一 *_PROTOCOL_VERSION，没有 minVersion/兼容矩阵。也没有任何上一版协议样本或“旧 R3 + 新驱动”回归。 |
| Q-05 | 需增强 | KswordARKLight/Core/DriverLease.cpp:10<br>KswordARKLight/Core/DriverLease.cpp:11<br>KswordARKLight/Core/DriverLease.cpp:175 | tests/native/KswordARKLightTests/KswordARKLightTests.cpp:48-52（OwnsStartTransition 两条 + Shoul | 租约是 KswordARKLight 私有的，连命名对象都带 KswordARKLight 前缀（DriverLease.cpp:10-11），Qt 主程序与 CLI 都不注册。DriverDock.Operation.cpp:4366/4487 的停止与删除服务路径不查任何租约，可以在 Light 仍持有句柄时把驱动停掉；反向也一样，Light 退出时的 ShouldStopOnLastRelease 只统计 Light 自己的租约数，不知道 Qt 还开着。需要把 DriverLease 提到 shared/ 并让三端共用，且给“预先运行的驱动不误认成自己的”写跨进程实测。 |
| Q-06 | 需增强 | tools/file_detail_tab_latency_test.cpp:1<br>tools/file_detail_tab_latency_test.cpp:27<br>tools/table_freeze_lifetime_test.cpp:1 | 无可执行测试。tools/file_detail_tab_latency_test.cpp 与 tools/table_freeze_lifetime_test | “UI 线程不同步等待”的实现纪律确实在（scheduleDeferredTabActivation + 1899 处 guardThis + 后台线程调用服务 API），但 Q-06 要求的测量能力完全没有：全仓搜不到任何 p95/percentile/event-loop 延迟采样器，只有 tools/file_detail_tab_latency_test.cpp 这种行为回归。需要新建可执行的延迟采样 harness（200 次选择/排序/筛选/取消，记录原始样本与 p95/max），并把两个孤儿 harness 补上 vcxproj 接进 CI。 |
| Q-07 | 尚未发现实现 ⟨复核：已确认无现成实现⟩ | Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverCallbackMonitor.cpp:54<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverCallbackMonitor.cpp:237<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverDebugOutput.cpp:52 | 无 | “算法吞吐 / 持久化吞吐 / 真实 collector 吞吐”三分与丢失记账的**协议字段**已经有（droppedCount 遍布 ArkDriverCallbackMonitor/DebugOutput/Audit/File），但 §7.2 的 L1–L5 固定负载完全没有落点：全仓搜不到固定 seed 生成器（无 mt19937/--seed/kSeed）、没有 100,000 实体或 1,000,000 事件的回放入口、没有吞吐/丢失/峰值内存的测量记录器。唯一的持续负载工具是 HVM 专用的 SOAK（ArkDriverHvm.cpp:61），只测 devirtualize 次数，不测事件吞吐。需要新建带 seed 的 fixture 生成器 + 经生产入口回放的 harness。 |
| Q-08 | 需增强 | Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverClient.cpp:28<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverClient.cpp:56<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverClient.cpp:278 | 无（KswordARKLightTests 覆盖 EvidenceSession::erase 但不做资源计数；无 50 次循环 harness） | “停止后归还”的机制在（in-flight 租约 + idle condition + RAII 句柄 + 后台 retire），但 Q-08 要的**可枚举账目**没有：全仓没有 tracked live allocations/handles/tasks 的计数入口（GetProcessHandleCount 只在 ksword/process/process.cpp:3580/3678 与 OtherDock.cpp:1952 用于展示别的进程，不是自检），没有 50 次启动/停止/关闭循环 harness，也没有缓存上限声明（file_handle_tools.h:151 的 nameResolveBudget 是单次查询预算，不是缓存上限）。 |
| Q-09 | 需增强 | Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverClient.cpp:120<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverClient.cpp:509<br>Ksword5.1/Ksword5.1/ArkDriverClient/ArkDriverError.cpp | 无自动化故障测试；仅 tools/driver_functional_ci/driver_test_plan.json:16 的 timeout 期望（“进程无 | 六类错误里只有“权限拒绝”“驱动忙”“页面被关闭”有生产实现；缺三样：(1) 没有任何故障注入测试适配层 —— 全仓无 fault-inject/simulateFailure/TestHook 之类的可注入接缝，也没有 class Fake/Mock/Stub 测试替身；(2) 磁盘满（ERROR_DISK_FULL）与响应损坏在全仓零命中，只有 corruptLinkCount/corruptBucketCount 这类**驱动上报的**内核链表损坏计数（ArkDriverAudit.cpp:1367/1435），不是 R3 对损坏响应的防御；(3) 断符号网只有 DumpSymbolResolver 的 FakeProcessHandle，没有断网路径测试。 |
| Q-10 | 需增强 | tools/driver_functional_ci/plan_gate.py<br>tools/driver_functional_ci/driver_test_plan.json:6<br>tools/driver_functional_ci/driver_test_plan.json:19 | KswordARKDriver/tests/DriverFunctionalMatrix.ps1 -Mode SelfTest（harness 安全逻辑自检，普 | 参数边界与并发退出的执行器很完整（三层归因、targetGuard、journal WriteThrough、上一轮崩溃阻塞），但 Q-10 明确点名的 **Driver Verifier 完全没有接入**：全仓 grep verifier 只命中 Ksword5.1/Ksword5.1/ksword/startup/startup.cpp:2549-2801 的 IFEO Application Verifier **检测**功能，DriverFunctionalMatrix.ps1 与 driver-functional-ci.yml 里零命中，也就没有“仅对指定 KSword 测试驱动启用、记录选项与运行时长”的机制。此外引用泄漏（对象引用计数）没有任何断言，只靠“不崩溃”兜底。 |
| Q-11 | 需增强 | KswordARKDriver/src/features/hvm/hvm_runtime.c<br>KswordARKDriver/src/features/hvm/hvm_resident.c<br>KswordARKDriver/src/features/hvm/hvm_internal.h | 无自动化。docs/zh-CN/virtualization-roadmap.md:138/406 记录的是“睡眠/唤醒一轮后重新 SOAK 通过”的人工验收口径。 | 只有 HVM 一条路径做了睡眠边界（PowerTransitionGeneration + PO_CB_SYSTEM_STATE_LOCK 出现在 7 个驱动文件里），其余持续采集路径（CallbackMonitor drain、DebugOutput drain、FileMonitor、ETW）没有任何电源转换处理。R3 侧全仓零命中 WM_POWERBROADCAST / PBT_APMRESUMEAUTOMATIC / PBT_APMSUSPEND —— Qt 主窗口不订阅电源广播，睡眠后采集会话与时间映射既不重建也不显式标记“已停止”，符合 Q-11 说的“假连续时间线”风险。 |
| Q-12 | 需增强 | tests/native/KswordFsDecodeTests/KswordFsDecodeTests.cpp:1<br>tests/native/KswordFsDecodeTests/KswordFsDecodeTests.cpp:139<br>Ksword5.1/Ksword5.1/FileDock/NtfsRunListDecode.h | KswordFsDecodeTests（runlist 越界/负 LCN/INT64_MIN/稀疏与终止符组合，全部作为不可信磁盘输入）；tools/scann | 具体三处：(1) KernelDock.CallbackRuleSerializer.cpp:317 importCallbackConfigFromJson 全函数没有任何 kMax 常量 —— 无输入字节上限、无嵌套深度上限、无 groups/rules 条数上限，重复 id 只在警告里忽略；(2) 全仓没有路径穿越归一化校验（搜不到 "..\\" 归一化或 canonical path 校验），导出目录约束靠 QFileDialog 而非代码判据；(3) HTML 转义是逐处 toHtmlEscaped（MinidumpDock.Tables.cpp:362、HardwareDock.cpp:8191、KernelDock.CallbackPromptManager.cpp:1080），没有统一门禁，而 setTextFormat(Qt::RichText) 在 HardwareDock.cpp:4256、KernelDock.CallbackPromptManager.cpp:500 直接开启。另外没有固定畸形样本集。 |
| Q-13 | 需增强 | tools/i18n_language_pack.py:586<br>tools/i18n_language_pack.py:625<br>tools/test_i18n_language_pack.py | tools/test_i18n_language_pack.py（i18n 工具自身单测）；tools/theme_token_audit.py --self- | 两个真缺口：(1) i18n 审计的 --source-root 硬钉在 Ksword5.1/Ksword5.1（vcxproj:820 与 ci.yml:234 两处），KswordARKLight 的 174 个 .cpp 全是硬编码中文宽字符串（App/MainWindow.cpp、Features/Kernel/KernelPage.cpp、Features/Driver/DriverUnloadedView.cpp 等，KswordARKLight.vcxproj 里连 languages 条目都没有），KswordCLI 同样零 i18n —— 本轮若在 Light 加入口，必然违反“没有裸翻译 key/硬编码术语墙”；(2) “未采集/无结果/权限原因”没有统一表达：shared/evidence/EvidenceEnvelope.h:22 的 CollectionStatus{NotCollected,Partial,Unsupported,AccessDenied,...} 正是为此写的，但该文件未接入任何工程也无任何引用，现状仍是各页面自己拼 setPlaceholderText 字面量。 |
| Q-14 | 需增强 | .github/workflows/ci.yml:38<br>.github/workflows/ci.yml:100<br>.github/workflows/ci.yml:108 | ci.yml:108 与 driver-ci.yml:87 的路径正则回归断言（防止有人把关键输入从触发集里删掉） | 现有门禁只保证“该构建的构建了”，不保证“改动没超范围”。缺三样：(1) 没有任何“改动文件 ↔ 验收编号”的映射产物或校验脚本，docs/next/KSword_Acceptance_Results_Template.json 里也没有 changed_paths 字段；(2) git diff --check（ci.yml:157）只报尾随空白与冲突标记，检不出无关重排/整文件重格式化 —— 而 .claude/memory/ksword-driver-crlf-index-trap 记录的 CRLF 索引陷阱恰好会让这类改动看起来是整文件 diff；(3) 许可证文件、目录结构、工具链版本无任何变更告警（Directory.Build.props 与 third_party/*/LICENSE.txt 改动不触发任何检查）。 |
| Q-15 | 需增强 | docs/next/KSword_Acceptance_Results_Template.json:10<br>docs/next/KSword_Acceptance_Results_Template.json:28<br>docs/next/KSword_Acceptance_Results_Template.json:39 | tools/validate_kernel_knowledge.py（同类模式的先例：对知识链做结构化校验并在 ci.yml:241 强制执行） | 模板齐全但没有任何执行侧保障：(1) docs/next/ 整个目录当前是 untracked（git status 显示 ?? docs/next/），ci.yml:217 用 git ls-files 取路径，因此这份模板连语法校验都不过；(2) 没有校验脚本落实 pass_requires —— 无人检查“verification_status=PASS 时 evidence_paths 非空且路径存在”“BLOCKED 必须有 blocking_reason”，可以随手把 116 条全改成 PASS 而不被发现；(3) docs/research/kernel-knowledge-acceptance.zh-CN.md 是上一轮同类产物，纯手工 markdown，同样无校验，可作为反面参照。补法：照 tools/validate_kernel_knowledge.py 的模式写一个 results 校验器接进 ci.yml。 |
| Q-16 | 需增强 | docs/zh-CN/virtualization-roadmap.md:184<br>docs/zh-CN/virtualization-roadmap.md:196<br>docs/zh-CN/virtualization-roadmap.md:203 | 无（KvmControl.cpp / KernelHvmTab*.cpp / ArkDriverHvm.cpp 均无任何测试覆盖；驱动侧 HVM 只有 driv | 边界文档本身很硬：docs/zh-CN/virtualization-roadmap.md:391-392 逐项标注 P3.1 AMD 能力探测 ✅ / P3.2–P3.7 SVM 后端 ⬜「刻意未写，需要 AMD 测试机」，:353 明确「未在真实 AMD 硬件通过前不发布 active 状态」。缺的是护栏：(1) 没有任何自动门禁防止本轮 diff 改写 shared/driver/KswordArkHvmIoctl.h 或路线图状态表 —— tools/ioctl_audit/ksword_ioctl_audit.py 只审 FILE_ANY_ACCESS 与关键词，不比对 HVM 协议版本；(2) Q-16 要求“只读接入既有事件/能力需要新增测试”，而最近一轮已经把 HVM 事件流接进 UI（Ksword5.1/Ksword5.1/UI/KvmControl.cpp、MainWindow.Kvm.cpp）却零测试，且 driver_test_plan.json 把 CONTROL_HVM 整条排除，功能矩阵也覆盖不到。 |

**本模块最该补的三件事**

1. 把已经写好但没接线的东西接上，这是零成本最高收益的一步。具体三件：(a) shared/evidence/{EvidenceEnvelope,LosslessValue}.{h,cpp} 已在工作树、代码质量完整（CollectionStatus/AnalysisConclusion/CoverageAccount 正是 Q-13 空状态与 Q-02 边界语义要的），但未进任何 vcxproj/filters 且全仓零引用 —— 加进 Ksword5.1/Ksword5.1/Ksword5.1.vcxproj 与 KswordARKLight.vcxproj；(b) tools/scanner_tests/scanner_self_test.cpp、tools/file_detail_tab_latency_test.cpp、tools/table_freeze_lifetime_test.cpp、tools/file_handle_scan_fallback_test.cpp 四个带 main() 的 harness 从来没被编译过，补 vcxproj 并接进 .github/workflows/ci.yml 的 usermode-release job（照 :355-365 跑 KswordFsDecodeTests 的写法）；(c) 把 KswordARKLightTests.vcxproj 与 KswordFsDecodeTests.vcxproj 加进 Ksword5.1/Ksword5.1.sln，否则本机构建永远不跑测试。命中 Q-01/Q-02/Q-03/Q-12。

2. 把 KswordARKLight/Core/DriverLease.cpp 提升为三端共用的共享租约，并让 Qt 侧 Ksword5.1/Ksword5.1/DriverDock/DriverDock.Operation.cpp:4366(停服务)/4487(删服务) 在动手前查租约。现在租约的命名对象是 Local\\KswordARKLight.DriverLease.Mutex.v1（DriverLease.cpp:10-11），只有 Light 注册，Qt 能把 Light 正在用的驱动直接停掉 —— 这是 Q-05 唯一一条“通过条件”的正面违反，且纯 R3 改动、无需硬件。纯决策已经在 DriverLeasePolicy.h 里且有测试（KswordARKLightTests.cpp:48-52），搬迁风险低。同时修 ArkDriverClient/ArkDriverCapability.cpp:63 的严格版本相等（旧驱动被判 header invalid 而非降级），把 Q-04 一起带过。

3. 补两个“证明层”的缺口，它们决定 Q-15 能不能诚实结账：(a) 照 tools/validate_kernel_knowledge.py 的模式写一个 docs/next/KSword_Acceptance_Results_Template.json 校验器 —— 强制 verification_status=PASS 必须有非空且真实存在的 evidence_paths、BLOCKED 必须有 blocking_reason，接进 .github/workflows/ci.yml 的 source-integrity job；顺带把 docs/next/ 纳入 git 跟踪，否则 ci.yml:217 的 Validate tracked JSON 连语法都不校验；(b) 在 .github/workflows/driver-functional-ci.yml 的 functional-matrix job 里加 Driver Verifier 段（仅对 KswordARK.sys 单驱动、仅自托管测试签名机、记录 flags 与运行时长）—— 现在全仓 verifier 只命中 Ksword5.1/Ksword5.1/ksword/startup/startup.cpp:2549 的 IFEO 检测功能，Q-10 点名的验证器一次都没跑过。注意 §9 明确“开启 Verifier 需独立授权”，因此这条要作为可选 workflow_dispatch 输入而非默认开启。
